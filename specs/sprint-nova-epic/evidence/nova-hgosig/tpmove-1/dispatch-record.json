{
  "schema": "pipeline.dispatch-record.v1",
  "taskId": "NOVA-HGOSIG-TPMOVE-1",
  "model": "claude-opus-5",
  "effort": "xhigh",
  "rulesetSha": "2914a55713d714d95323231edaddccec5e1d6c2e5a4fbfaaaaa9d913a5fb1efc",
  "dispatcher": "goldfish-deep",
  "baseCommit": "105788d",
  "branch": "feat/sprint-nova-codex-v046",
  "committed": false,
  "window": "signed guard maintenance window over TP-2/TP-6/TP-7, expiring 2026-08-07T21:47:45.930Z; verified read-only via windowCoversRule() before any write (TP-1 and TP-3 confirmed NOT covered). No window was opened, extended, installed or closed by this dispatch.",
  "outcome": "D1: moved the two guard-spawning cases out of lib/human-guard-override.test.mjs into the suites that own the guards they exercise -- guard-testpath.test.mjs (TP12/TP13, rewritten into that suite's check(id, tool, path, exit, opts) idiom) and guard-gate-strength.test.mjs (GST30, rewritten into its check(name, callback) + assert idiom). The library suite keeps exactly its three renderer-level unit cases. D2: OT13 in guard-testpath-override.test.mjs renamed and rebuilt as a DRIFT test that asserts {status: 'replan', code: 'HGO-DRIFT'} by name via a direct consumeHumanGuardOverride() call, plus a byte-identical twin fixture that does admit and consume; OT19 added for authorizeHumanGuardOverride()'s HGO-SIGNATURE-MODE-REQUIRED check, which survives ADR-0059 Decision 3 one layer earlier.",
  "changedFiles": [
    "plugins/pipeline-core/hooks/guard-testpath.test.mjs (+74/-2, TP-2, under the window)",
    "plugins/pipeline-core/hooks/guard-gate-strength.test.mjs (+32/-0, TP-6, under the window)",
    "plugins/pipeline-core/hooks/guard-testpath-override.test.mjs (+124/-6, TP-7, under the window)",
    "plugins/pipeline-core/lib/human-guard-override.test.mjs (+7/-87, unprotected)"
  ],
  "suiteResults": {
    "guard-testpath.test.mjs": "13/13 cases passed (was 11)",
    "guard-gate-strength.test.mjs": "30 passed, 0 failed (was 29)",
    "guard-testpath-override.test.mjs": "19 passed, 0 failed (was 18)",
    "lib/human-guard-override.test.mjs": "26 pass, 0 fail (was 28; two cases moved out)"
  },
  "redEvidence": "evidence/nova-hgosig/tpmove-1/{control,mutation-drift,mutation-status-digest,mutation-always-drift,mutation-mode}.log + summary.json, produced by evidence/nova-hgosig/tpmove-1/run-mutants.mjs. No tracked file was reverted, stashed or checked out: the harness copies plugins/pipeline-core into the gitignored evidence/ tree, asserts the copied suite is byte-identical to the tracked one, mutates only the COPY's lib, and runs the real suite file from there. control = 19/19 green. drift (repository observation dropped from the `drifted` comparison) and status-digest (statusSha256 replaced by a constant) each redden OT13 alone. always-drift (consumption refuses everything) reddens ONLY OT13's twin half while its drift half stays green -- the demonstration that the differential is load-bearing. mode (Decision 1 check disabled) reddens OT19 alone.",
  "verifyGate": "NOT OBTAINED, by briefing contradiction. node harness/scripts/verify.mjs exits 1 at VERIFY-CANDIDATE-PREFLIGHT ('Commit or stash tracked changes before Verify; no suite was started') because the four test files are uncommitted, and this briefing prohibits both committing and any tracked-file mutation. Machine evidence of the refusal: evidence/verify-latest.json. The dispatching Elephant confirmed mid-task that this is expected and that it runs Verify itself after committing; substitute evidence was explicitly waived.",
  "lineReferenceCorrection": "The briefing placed the HGO-SIGNATURE-MODE-REQUIRED check at lib/human-guard-override.mjs:1477-1482. It is at :1550-1558 (mode read 1550-1552, fail() 1553-1558), inside authorizeHumanGuardOverride() which opens at :1527, immediately after the `activate !== true` check at :1541. The prescribed SHAPE was correct and was verified against the code; only the line numbers were stale.",
  "scopeNotes": "No production code touched. No other case in any of the four suites weakened, skipped or deleted. guard-testpath.test.mjs's shared check() helper gained one ADDITIVE optional channel, stderrMatches (RegExp list), so the moved TP13 keeps its 64-hex request-digest shape assertion rather than degrading to a substring; existing cases pass none and are unaffected. No ADR, docs/state.md or backlog item touched. TP-1/TP-3/TP-4/TP-5/TP-8/TP-9/TP-10 untouched and still enforced."
}
