{
  "schema": "pipeline.dispatch-record.v1",
  "taskId": "CRITIC-REMEDY-09",
  "model": "sonnet",
  "rulesetSha": "2914a55713d714d95323231edaddccec5e1d6c2e5a4fbfaaaaa9d913a5fb1efc",
  "dispatcher": "goldfish-deep",
  "outcome": "F1 (major, live regression): localPluginInstallSourceObservation() in plugins/pipeline-core/lib/human-guard-override.mjs required this checkout's OWN .claude-plugin/marketplace.json to self-name 'agent-pipeline-local', which the prior candidate's marketplace-rename (correctly restoring 'agent-pipeline' as the published identity, ADR-0052) made permanently false, making HGO-PLUGIN-SOURCE unreachable-pass and the sanctioned local-plugin-install override dead; fixed by requiring the corrected published name 'agent-pipeline' instead (the checkout's own manifest never legitimately self-names 'agent-pipeline-local' post-ADR-0052; that name is reserved for the separate external local-marketplace root the ADR mandates), which is a real, non-tautological attestation (still fail-closed against a tampered/incorrect manifest) rather than a weakening; closed the accompanying test blindness by exposing isPipelineSourceRoot/localPluginInstallSourceObservation via humanGuardOverrideInternals and adding a new test in plugins/pipeline-core/lib/human-guard-override.test.mjs that exercises the attestation directly against THIS repository's own real marketplace.json/plugins/pipeline-core tree (not a synthetic fixture), proven live: with the real manifest's name temporarily changed the suite went red (exit 1, HGO-PLUGIN-SOURCE) and after restoring it byte-identical (confirmed via git diff --stat, zero output) the suite went green (exit 0) -- manifest never left altered at end of any turn; updated the two pre-existing synthetic-fixture tests' marketplace name field from the now-incorrect 'agent-pipeline-local' to 'agent-pipeline' to match a legitimate post-ADR-0052 checkout shape; on the second, structural half (pluginSourceTreeSha256 hard-failing on any symlink vs. the ADR-0052 local-dev symlink arrangement): traced reachability and found NO actual incompatibility -- the ADR-0052 symlink lives only at the SEPARATE, external local-marketplace root's OWN plugins/pipeline-core entry, never inside this checkout; pluginSourceTreeSha256 only ever walks <repo.root>/plugins/pipeline-core where repo.root is always this checkout's own physical (symlink-free per physicalRoot()) top-level, and localPluginInstallSourceObservation already requires that exact directory to be real/non-symlinked before the walk begins; the external marketplace root can also never become repo.root for this guard flow because isPipelineSourceRoot() additionally requires harness/scripts/verify.mjs, which the external root never has; documented this reachability trace as a code comment above pluginSourceTreeSha256 rather than changing its fail-closed symlink rejection, since no reachable incompatibility exists to fix and weakening it would be exactly the forbidden move; no change to ADR-0052 (not in the allowed edit list; Decision untouched). F2 (major, false ADR consequence): confirmed via project-authority.mjs trace and reproduction that resolveProjectAuthorityPaths()'s 'missing' status is keyed off the manifest (pipeline.yaml) alone at both tiers and does not distinguish a genuinely pristine project from a project holding only a legacy calibration (.claude/pipeline.json, CLAUDE.md-documented as never having adopted the optional manifest); judged the generator's mapping to be the defect (not the ADR's Consequences text) because seeding such a project at the neutral tier orphans a live, still-read .claude/pipeline.json -- exactly the drift class ADR-0053's own Context exists to prevent -- so fixed resolveCompiledRuntimeTargets() in setup.mjs to check directly for a present legacy calibration file before honoring a 'missing' status as neutral-eligible pristine, keeping such a project on the legacy tier; only a project with no legacy calibration either (genuinely pristine) still seeds neutral, preserving Decision 1's stated intent and keeping the Consequences claim ('a legacy-tier consumer project is never silently migrated by running setup.mjs') literally true rather than needing correction; added a dedicated setup.test.mjs test pinning this; did not touch project-authority.mjs (out of scope/forbidden) or rewrite ADR-0053's Decision section; added a dated remediation note to ADR-0053's Context documenting the discovered edge case and the fix, for the historical record. F4 (minor, stale comment): guard-lifecycle-ready.mjs's runner-identity invariant comment claimed exactly one production caller (codex-pretool-guard.mjs's boundedSpawn); corrected it to enumerate both current callers, also naming guard-apply-patch.mjs's GUARDS spawn list (plugins/pipeline-core/hooks/guard-apply-patch.mjs:18), which already always supplies --runner codex and which guard-apply-patch.test.mjs already independently pins as 'second production caller'. All DoD checks green: human-guard-override.test.mjs 18/18 (exit 0), setup.test.mjs 187/187 (exit 0), project-authority.test.mjs 20/20 (exit 0), guard-lifecycle-ready.test.mjs (exit 0), guard-apply-patch.test.mjs 9/9 (exit 0), check-doc-contracts.mjs (exit 0), git diff --check clean. Deviation noted and accepted as an unambiguous typo, not a stop condition: the briefing's DoD/Forbidden/finding text repeatedly cites 'plugins/pipeline-core/hooks/human-guard-override.test.mjs', but the only such file in the repository is at plugins/pipeline-core/lib/human-guard-override.test.mjs (paired with lib/human-guard-override.mjs, which the briefing correctly cites at that lib path) -- treated as the intended file since no second candidate exists anywhere in the repo."
}
