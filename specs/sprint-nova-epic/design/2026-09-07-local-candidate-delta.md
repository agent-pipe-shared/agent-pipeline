# Local candidate delta — 7 September 2026

This contract supplements the Sprint Nova PRD for the next local test candidate.
The release target is 0.6.2; a local candidate retains the current release base
and receives runner-specific build metadata. Release publication remains outside
this delivery. The PO now authorizes pushing the final tested local candidate to
the existing feature branch. D.2–D.6 followed the first test candidate; the current user goal
includes them in the next local candidate, before the 0.6.2 release.

## Current documentation completion scope — 9 September

- Complete D.2 capability coverage and its actual public targets. Activate the
  inventory only with a genuine passing Critic receipt; fixture digests are not
  review evidence. Include the inventory and its checker in technical review.
- Complete D.3 frontdoors, D.4 parallel-work guidance and D.5 cost documentation
  with the delivered runner boundaries and evidence limits stated accurately.
- D.6 attribution must preserve supplied labels and distinguish measured runs
  from estimates. Missing comparable runner measurements remain an explicit
  evidence dependency; do not manufacture a comparison to close the item.
- Perform the fresh two-stage reader review prescribed by
  `harness/reader-review-protocol.md` after inventory activation, resolve findings,
  and bind its closure to the final committed documentation state. Include the
  reader checker, release-preflight integration and regressions in Critic scope.
- Complete the started Nova-B repairs, including GG22 pathspec regressions and
  current preflight-schema compatibility binding, before the final local stamp.

## Routing authority

- The validated project V3 source is the sole authority for active runner duty
  model and effort. Generated compatibility projections must agree with it.
- Claude Fable 5.1 and floating Fable selections are excluded from active routes,
  including fallbacks. Retain anonymous `AI-Assisted: true` attribution.
- Codex selected and normal Critic invocation, advisory invocation and review
  model-readiness checks use the applicable configured model. Duty-specific
  effort accompanies real invocation; generic sandbox receipts retain their
  closed runner/model contract.
- Resolve candidate-bound review routes from the frozen candidate source.
  Reject missing, invalid or unavailable authority and mismatched response
  identity. A fallback may be selected only if the governing route declares it.
- Distinguish configured/requested routing from observed provider/model facts;
  configuration alone does not prove effective execution identity.

## Parallel work and bootstrap

- ADR-0080 governs the slicing advisory. Single-task dispatch and planning events
  produce its machine hint under its declared threshold and state rules.
  Slicing remains advisory; it introduces no hard parallelization gate.
- Deliver that advisory through the native Claude, Codex and Antigravity
  integration surfaces. Preserve unrelated hook registrations and runner event
  formats. Prefer available native dispatch tools to a new workflow executor.
- Preserve deduplication, bounded local bookkeeping, malformed-input handling
  and the distinction between fixture validation and live host delivery.
- The bootstrap guard identifies dispatched agents using the actual supported
  runtime identity. Measure receipt absence before activation and provide the
  receipt path before enforcing the first-write check. A receipt must not claim
  successful comprehension when it proves only invocation of a bound preflight.

## Greenfield authoring and recovery

- A fresh consumer can retain its original requirements, collect missing setup
  answers together, generate PRD/spec scaffolding, author those generated paths
  and keep bounded temporary notes while bootstrap binding is pending.
- Original material, setup settings and recovered summaries remain distinct.
  Preserve original bytes and capture order; never present a recovered summary
  as a verbatim user quotation. Use source pointers and collect specific unresolved
  confirmations where original material is unavailable.
- Concurrent real session-capability probes must not report missing capability
  merely because another valid probe publishes or retires its descriptor.
  Remove only owned temporary artifacts and empty directories; preserve foreign
  state and genuine permission, identity and path failures.
- Scratch admission at intended onboarding stages retains traversal, symlink,
  authority-path and readiness boundaries. It does not authorize implementation
  before the actual human plan-approval gate.
- A native multi-file patch through the guard chain must admit legitimate
  generated document and scratch authoring. Validate this with a genuine generated
  intake checkpoint and real session probes; disclose any simulated host boundary.
  A fixture result must not be reported as a completed live user-session run.
- `GUARD-LIFECYCLE-NOT-READY` is nonliftable, alone or in a mixed denial set.
  Neither a new override plan nor a previously armed capability may bypass it.
  Preserve legitimate override ceremonies for other eligible denial classes.
- Recovery output states the failed ability and a usable next action or technical
  repair requirement. Repeating an unchanged inspection is not represented as a
  repair. Nonliftable failures offer no signing ceremony. Keep diagnostics bounded
  and distinguish plugin output from host rendering of the original tool request.
- Follow onboarding's current returned action. Do not insert later state or
  authority-orientation steps as unreachable pre-binding prerequisites.

## Review, evidence and delivery

- The repaired model-free Codex preflight preserves stdin until initialization
  can complete and terminates its child within the declared bound.
- A real independent Codex Critic review follows an exact, clean full Verify gate,
  frozen-candidate dispatch admission and usable selected transport. Do not
  substitute a fixture, generic CLI fallback or unsupported isolation claim.
- Bind review and verification evidence to the exact candidate commit/tree.
  Preserve completed-but-invalid execution facts separately from no-child results.
- GitLab activity is restricted to reads using the approved operator-local
  authentication boundary. Attribute imported observations to their actual source;
  do not claim a new local execution, CI run, lease or worker proof from read access.
  Keep credentials and private project/account coordinates out of tracked evidence.
- Record PO decisions and residual technical or human-only blockers durably.
  Queue item 11 needs no repeated approval. Reader review belongs in the
  documentation block, with a later cheap documentation-state binding; ADR
  governing-path coverage precedes reliance on that binding. CR work stays in
  Nightwing and the supervisor provider probe remains deferred.
- Stamp and verify the local candidate, report its exact identity and remaining
  limitations, and provide the test handover. Do not claim the active installed
  plugin changed solely because the source candidate changed.

## Changed surface boundary

The review covers the fixed candidate diff and its direct contracts: runner
registry/projections and duty routing, native dispatch hooks, bootstrap identity
and receipt enforcement, session probes and lifecycle/override guards, resume
entry guidance, Codex preflight and Critic/advisory transports, and the associated
tests, capability inventory, backlog decisions and evidence. Historical isolated
experiments do not become active model-routing authority through this delta.

## Additional confirmed handover CLI repair — 8 September

The AFK continuation exposed a concrete defect in the existing handover tool:
with valid section selection and extraction acknowledgement, `--dry-run`,
`--check-size` and unknown options were ignored and the command performed a
real rotation. The reproduction used disposable fixtures only; evidence is
`backlog/evidence/2026-09-08-handover-size-command-mismatch.md` and
`scratch/handover-cli-ignored-mode-reproduction-20260908.json`.

The candidate repair must give the published inspection modes explicit,
read-only behavior using the existing configuration, measurement and rotation
primitives. A size check measures the configured file against its configured
byte limit. A dry run validates current section-bound extraction acknowledgements
and describes the proposed archive without writing any file or directory.
Unknown options, missing values and conflicting modes must refuse before
mutation. Preserve existing deliberate rotation and acknowledgement syntax,
path containment and the distinction between the two archive mechanisms.
Regression evidence must exercise the actual CLI and compare complete fixture
state before and after each inspection/refusal, including private markers and
governance registration. Update the close instructions to match the executable
contract. This adds the CLI, its direct tests and the bounded close-instruction
change to the final candidate review; it authorizes no rotation of this session.
