# Nova current-candidate issue acceptance matrix

This matrix reconciles the 17-issue Nova scope against the post-v0.4.7
candidate. “Implemented” means a contract/kernel or focused test is present;
it is not evidence of acceptance or closure. “Evidenced/closed” is reserved
for a fresh, candidate-bound acceptance gate, which the current records do not
claim. The released v0.4.7/#63 material is baseline history only.

## Candidate binding protocol

This document deliberately does **not** name the mutable working tree as a
candidate. After the final Nova candidate commit is created, append one
immutable record here with its full commit SHA, tree SHA, branch, matrix digest
and creation time. Every Verify, Security, Critic, PO-gate and publication
receipt used by this matrix must repeat that exact commit/tree pair and matrix
digest. A changed commit, tree or matrix requires a new record and fresh
receipts; historical receipts remain history rather than evidence for the new
candidate.

## Candidate binding records

| Candidate commit | Candidate tree | Branch | Matrix preimage SHA-256 | Created at |
| --- | --- | --- | --- | --- |
| `8ce8d5d3601feed226333451092b8fc06b6dd4a2` | `41a495f2cda4eaa4617e74581a3b094fe56b9ce6` | `feat/sprint-nova-codex-v046` | `3efaf5094617b212baf5f9e28a78dc8874812d87a0bea94fdf31502b90baf2b3` | `2026-08-01T12:11:03+02:00` |
| `92039bbbc834e69d8474d2da87d5510bb55ca522` | `f24e6f45e4d1c841a7acd6446f9b9ea0e04edd00` | `feat/sprint-nova-codex-v046` | `10d0ab426f1b6edb4c0abbb75c72b5e6dcb498c79e0ecfd6fe4497a9912a6936` | `2026-08-18T09:20:11+02:00` |

The 2026-08-01 record above is superseded, not deleted (immutable
history). This second record binds the exact code candidate — including
the `0.6.0` version bump, the full Sentinel/Cyborg backlog reconciliation
(7 items closed, one real two-round security fix cycle), and the
closure_commit format regression Verify caught and this same block
fixed — a fresh clean Full Verify (268/269, only the known,
separately-tracked `human-guard-override-tests` host-config exception)
ran against exactly this commit/tree; `evidence/verify-latest.json` binds
it. This record binds the immutable code candidate before this
documentation-only append. A Critic, PO-gate or publication record must
repeat the same pair and this matrix preimage digest. **Not yet
sealed:** no candidate-bound zero-findings comprehensive Critic evidence
and no PO release-preflight consent exist yet for this exact commit —
both remain open per every issue row's own "Only the candidate-freeze +
gate chain remains" language above; NVA-A8-5's pilot execution stays
genuinely PO-gated regardless of this freeze.

The record above is historical only. Later Nova commits, including the B0
payload-budget and close-boundary corrections, invalidate it as a current
candidate binding. No post-rebase final Nova candidate is frozen yet.

| Issue / block | Accepted/current scope | Existing implementation or evidence | Remaining gap | Exact next verification / closure gate | Disposition |
| --- | --- | --- | --- | --- | --- |
| #57 / Nova A1 canonical reconciliation | Versioned Spec-bound delivery record; sanctioned append-only assignment/closure; CAS idempotence; portfolio snapshot; append-only repair of events 39/40; generic initializer. | A1 writer/reconciliation suites and historical backlog evidence remain intact. A fresh, schema-valid binding of the canonical 37-item snapshot, current ledger head and Spec is sealed at `specs/sprint-nova-epic/evidence/backlog/backlog-spec-bindings-a1-8c24a1f.json` (path corrected 2026-08-07; the bare `evidence/backlog/...` form does not exist, that top-level directory is gitignored/regenerated-only); it binds all NVA-A57-1..6 without rewriting the historical transition evidence. Checker-green and events-39/40 amendment readback, the row's prior "remaining gap," were separately closed by the 2026-08-06 ledger-reconciliation work (`2dc9511`/`def7e59`): `check-backlog-state.mjs` runs green, `reconcile-backlog-ledger-tests`/`backlog-state-check` are registered Verify steps, `reconcile-backlog-ledger.test.mjs` (11/11) and `reconcile-backlog-delivery.test.mjs` (6/6) cover the stale/replay/partial and generic-initializer cases. Found and reconciled 2026-08-07 during Nova A status review, not new work. | Only the candidate-freeze + gate chain remains: no post-rebase Nova A candidate is frozen yet (this document's own binding-records table still only holds the invalidated 2026-08-01 record), no Critic evidence postdates 2026-08-02 (predates the 08-06 ledger work), and no PO increment-gate evidence exists. | Freeze a Nova A candidate, run a fresh Critic pass that actually covers the 08-06 ledger-reconciliation change, then bind Full Verify/Security/Critic and the PO increment gate. | Checker-green and amendment readback proven 2026-08-06; only candidate-freeze, fresh Critic and PO gate remain open. |
| #7 / Nova A2 runner conformance | Versioned capability matrix, synthetic offline certification, distinct fan-out/worker cells, non-empty baseline, typed usage/cost and assurance availability; #75 owns aggregation. | Capability/selected-sandbox contract suites (18/18, 11/11 and related focused runs) are historical pre-rebase evidence. The current candidate-bound report is sealed at `evidence/nova-a/a2/runner-capability-report-f7d33a5.json`: it has a non-empty synthetic baseline, one locally observed Codex read-only-preflight cell, explicit unavailable Claude and unsupported worker cells, and a successful opt-in live certification receipt on Codex `0.146.0`. Its compatibility is semantic and receipt-bound, never a static runner-version pin. It does not claim observed model identity or selected-child success. | Final Nova-A binding of the report into Verify/Security/Critic remains open; #75 aggregation and any unobserved runner capability remain outside this report. | Retain NVA-A7-1..6 evidence on the frozen final candidate, including typed unavailable fields, then bind the report into Nova A Verify/Security/Critic. | Candidate-bound local report sealed; final increment evidence open. |
| #8 / Nova A3 benchmark | Frozen five fixture classes, unchanged scoring, serial/native baselines, overhead decomposition, resource envelope; pilot input remains separately gated. | A fresh candidate-bound report is sealed at `evidence/nova-a/a6/multi-cli-benchmark-report-57ee7e9.json` (focused suite `6/6`). NVA-A8-1 (fixture versioning/reproducibility), NVA-A8-2 (unchanged, route-name-generic scoring), and NVA-A8-3 (six-way stage overhead decomposition) are each mapped to passing tests. **NVA-A8-4's empirical half is now real, not synthetic:** `specs/sprint-nova-epic/evidence/nova-a/a3/multi-cli-benchmark-record-718b019.json` (candidate `718b019e`, structurally valid per `evaluateMultiCliBenchmark`) ran all five fixture classes through both routes for real — `serial` a bare `node task.mjs` subprocess, `native` a real headless `claude -p` CLI call whose LLM invokes the identical script as a Bash tool call (30 real CLI invocations, 5 classes × 6 reps incl. warmup, per the PO's "Echte CLI-Calls, klein halten" decision) — with real hrtime-based `wallMs`/`cpuMs`, real native-route token usage, and `correctnessEqual: true`/`resourceWithinEnvelope: true` on every class. The scored `recommendation` came back `no-recommendation`: native wall time runs ~100–200x serial's (e.g. mini: 51ms median serial vs. 10913ms median native) because native pays a real LLM round trip the bare-script serial baseline does not — an honest, expected result of comparing dispatch paths exactly as designed (differ only in dispatch path, never task content), not a defect in either route or in the scoring contract. | NVA-A8-5 (the `pipeline.multi-cli-efficiency-pilots` gate) is still not demonstrated — no PO-gated pilot record exists, unaffected by this run, and this criterion's own text forbids closing PILOT EXECUTION via framework delivery. **Correction, 2026-08-18: the PILOT DESIGN half of this gap was never actually PO-gated, and was mischaracterized as such in this matrix and in `docs/state.md` for a week.** `backlog/items/2026-07-20-multi-cli-efficiency-pilots.md`'s own Ownership-and-expiry clause assigns triage and pilot design to the Elephant, reserving only execution/evaluation to the PO. The item had expired (2026-08-17) with an empty Triage section, unread by every prior characterization; renewed and triaged 2026-08-18 — both pilot hypotheses (wave-review, remote-mini-train) now have a complete, bounded design (baseline/route/cost unit/success threshold/stop condition/rollback) ready for the PO's go/no-go. Only the actual pilot RUN remains genuinely PO-gated. Post-rebase reproducibility/readback beyond the two sealed benchmark records and final Verify/Security/Critic binding into a frozen Nova A candidate also remain open, same as every other row. | Bind both sealed records into Nova A Verify/Security/Critic once a final candidate is frozen; the pilot design is done — what remains is the PO's own go/no-go on running either designed pilot for real, then the observed cost/route evidence from that run. | Implemented (provisional); open — NVA-A8-4 empirical evidence now real and sealed (2026-08-11); NVA-A8-5's pilot design now complete (2026-08-18), only its PO-gated execution remains; final candidate-freeze binding remains open. |
| #12 / Nova A4 bounded scheduling | Deterministic planner/waves, explicit rejection reasons, lifecycle next-wave receipts; no production executor without ADR approval. | A fresh candidate-bound report remains sealed at `specs/sprint-nova-epic/evidence/nova-a/a4/execution-plane-scheduling-report-57ee7e9.json` for NVA-A12-1..4 (`scheduling-lifecycle.test.mjs` 9/9, `parallel-dispatch-planner.test.mjs` 10/10, both re-verified unmodified). **Candidate-bound integration is now real, per ADR-0062 (the PO confirmed 2026-08-11 that ADR-0062 is sufficient authority, no further ADR needed):** `plugins/pipeline-core/scripts/execution-plane-launch.mjs` drives one real `local-worker-supervisor.mjs` run (`"fixture"` runner only — never `"codex-exec"`, never `allowProviderExecution` — the live-provider disposition, ADR-0048 B1-I, stays expired/unrenewed and untouched) through a real spawned child, normalizes the genuine outcome via a new `normalizeRealExecutionOutcome`/`reduceRealExecutionState` path (added alongside, not replacing, the synthetic one), and projects the resulting real terminal `NovaExecutionState` into `composeSchedulingLifecycle`'s `terminalOutcomes`. One real opt-in run is sealed at `specs/sprint-nova-epic/evidence/nova-a/a4/execution-plane-launch-record-4be63d36.json`: it genuinely reached a real `failed` outcome (fixture exit code 7) — an honest failure, not a fabricated success — and the package moved to `failed` in `scheduling-lifecycle`'s rev-1 record. The `nova-a4-e2e-companion` package is independent, not a dependent (both packages declare `dependencies: []`, and the launcher's own code comment calls it a second, independent package); it was blocked by resource contention (`PDP-BLOCKED-RESOURCE:nova-a4-e2e`), not by a dependency edge, and became `selected` once that resource freed up. Spec §5.4's transitive-dependent propagation path (a failed package's true dependents becoming terminal `upstream-failed:<packageId>`) was NOT exercised by this real run. **2026-08-18 — a real success-path run was attempted and found genuinely, architecturally unreachable, not merely un-run.** The launcher's hardcoded fixture exit code was parameterized (`--fixture-exit-code`, `execution-plane-launch.mjs`, 14/14 new unit checks, no change to the existing failure-path default) so a success case could be selected without editing the file. A REAL run with `--fixture-exit-code 0` genuinely spawned a real child that exited 0 — but `local-worker-supervisor.mjs` maps that to worker `completed`, which `execution-plane-contract.mjs`'s `normalizeRealExecutionOutcome` correctly maps to `succeeded-unverified` (by design — EPC08's own test title: "reserves verified for matching verifier evidence"). `succeeded-unverified` is in neither `execution-plane-contract.mjs`'s frozen `TERMINAL` set nor `scheduling-lifecycle.mjs`'s admitted terminal-outcome vocabulary (`{"verified"} ∪ TERMINAL_STATE`) — so the real run correctly throws (`SHAPE:terminal`) rather than silently admitting an unverified success. This is the same gap already named in this document's own 2026-08-11 finding ("no step of the ADR's own three-candidate plan removes that line... a new, real-outcome normalizer... a design decision... arguably beyond what ADR-0062 itself authorizes") — now independently reconfirmed from the executor side, not just the verify-resume side, and by actually attempting the run rather than reasoning about it. **This is not a missing test or a missing CLI flag — it is a live architectural question for the PO/ADR-0044 owner: what does "a real worker exited 0" mean to the frozen scheduling contract without a verifier?** The `--fixture-exit-code` flag exists and is ready the moment that question is answered; forcing a fabricated `verified` state to close this row was correctly refused (briefing NVA-A1214-SUCCESS-1, stop condition 2). | Final Nova-A binding into Verify/Security/Critic remains open (same precondition as every other row — no post-rebase candidate is frozen yet); a real success-path run is now confirmed architecturally blocked, not merely unattempted — it needs a design decision on how (or whether) `succeeded-unverified` can ever become a scheduling-lifecycle terminal outcome, which is ADR/PO territory, not a code change; the live-provider (`codex-exec`) path remains separately inactive/unrenewed by design, out of this row's scope. | Bind the report plus the real-launch evidence into Nova A Verify/Security/Critic once a final candidate is frozen; the success-path question is now a named open design decision (frozen-contract real-outcome normalizer, or accept the failure-only demonstration as sufficient for this row) rather than an unstarted task — PO/ADR-0044-owner call. | Implemented (provisional); open — production-executor wiring now real (2026-08-11) via the ADR-0062 launcher; the success-path gap is now precisely characterized (2026-08-18) as a design question, not an execution gap; final candidate-freeze binding remains open. |
| #14 / Nova A4 execution-plane contract | Versioned request/admission/state/heartbeat/cancel/timeout/result/evidence companions; frozen v1 exchange unchanged. | Same sealed report as #12 above for NVA-A14-1..5 (`execution-plane-contract.test.mjs` 10/10, re-verified unmodified). **The "workflow-boundary suites (31/31)" discrepancy is now reconciled (2026-08-17): it was a naming mixup, not a missing/uncorroborated suite.** `plugins/pipeline-core/lib/workflow-runner-boundary.test.mjs`, registered as `workflow-runner-boundary-tests`, genuinely has 31/31 passing tests (confirmed live) — earlier reconciliation attempts searched for a literal "workflow-boundary" name and never found this suite under its actual registered name. But reading `workflow-runner-boundary.mjs` itself confirms it is NOT part of NVA-A14's scope at all: it is a "provider-neutral synthetic workflow boundary" governing adapter dispatch validation (`taskId`/`mode`/`sideEffects`/`allowedPaths`/`isolation`/`guard`/`commandAllowlist`), an unrelated subsystem to `execution-plane-contract.mjs`'s scheduling/worker-execution state machine — the historical citation combining the two was a documentation error, not evidence of missing NVA-A14 work. **The frozen v1 exchange itself is genuinely unchanged** (`normalizeSyntheticExecutionOutcome`/`reduceExecutionState` untouched, confirmed via diff and their own unmodified 10/10 suite); a new sibling `normalizeRealExecutionOutcome`/`reduceRealExecutionState` path was added alongside it for real adapter coverage, with its own new suite `execution-plane-contract-real.test.mjs` (5/5): real `completed`/`failed`/`timed-out`/`cancelled` worker outcomes map onto the existing closed state machine, the `cancel` kind reuses the same two-step `cancel-requested`→`cancelled` handshake as the synthetic path, and the real supervisor's `recovery-required` status (which has no direct synthetic counterpart) is explicitly split by result-null-ness into `lostHeartbeat`→`lost` (identity genuinely lost) vs. `completedUndelivered`→`completed-undelivered` (a result exists but could not be trusted) — a disclosed judgment call, not a discovered fact. Real `observation.source`/`monotonicMs`/`wallTime` are genuine (not the synthetic path's `expected.revision + 1` counter trick — confirmed `monotonicMs` is not load-bearing for ordering anywhere in the contract, only `revision` is). Exercised end-to-end in the same real run as #12 above. | Adapter coverage against the real, ADR-0062-authorized production executor is now demonstrated (see #12); final Nova-A binding into Verify/Security/Critic remains open, same as every other row. | Bind into Nova A Verify/Security/Critic once a final candidate is frozen. | Implemented (provisional); open — real adapter coverage against the production executor now demonstrated (2026-08-11); the workflow-boundary-suite discrepancy reconciled as a naming mixup (2026-08-17); only final candidate-freeze binding remains. |
| #29 / Nova A5 selected sandbox | Fingerprint-bound resolver, bounded retry/invalidation, privacy-safe diagnostics, nonce-bound child receipt; no fallback/host-only false attestation. | A fresh candidate-bound report remains sealed at `evidence/nova-a/a2/selected-sandbox-report-57ee7e9.json` for NVA-A29-1..5 and -7 (`11/11` `selected-sandbox-disposition.test.mjs`, plus `13/13` `codex-sandbox-select.test.mjs` and host-capable `6/6` `critic-packet-preflight.test.mjs`). **NVA-A29-6's positive half is now real, per ADR-0062 (`#29` clause):** `plugins/pipeline-core/scripts/selected-sandbox-launch.mjs` is a new, standalone, opt-in launcher (never wired into any bootstrap/preflight/Verify entrypoint) that drives a real `probe-start` → real child spawn (`spawnSync`, a credential-free `node -e` child) → real `probe-success`/`probe-failure` sequence through the unmodified `reduceSelectedSandboxDisposition`/`createSelectedSandboxDisposition` reducer. Its fingerprint's 8 fields are computed at runtime from genuinely observed host/runtime facts (sha256 of the real Node binary bytes, sha256 of this host's real `/proc/sys/kernel/random/boot_id`, `process.platform`/`process.arch`, sha256 of the real applied spawn-isolation config, sha256 of the launcher's own real source bytes, sha256 of the asserted policy statement) — never arbitrary constants. One real opt-in invocation on this candidate (commit `c8cbc01c`) genuinely reached `available-attested`: sealed at `specs/sprint-nova-epic/evidence/nova-a/a2/selected-sandbox-launch-record-c8cbc01c.json`, independently re-validated `{"ok":true,"code":null}` against the unmodified `validateSelectedSandboxDisposition`. The frozen negative corpus (`selected-sandbox-disposition.test.mjs`, NVA-A29-6's negative half) is unchanged and still passes as-is; a new `selected-sandbox-launch.test.mjs` (4/4, mocked spawn) covers the launcher's probe-start→probe-success and probe-start→probe-failure paths without a real process invocation. **A second real opt-in invocation — 2026-08-18, candidate `7936a98`:** run with the identical command, again reached `available-attested`; sealed at `specs/sprint-nova-epic/evidence/nova-a/a2/selected-sandbox-launch-record-7936a98.json`. Confirmed genuinely independent, not a replay: distinct `attemptId`, `nonceSha256`, `childIdSha256` and `recordSha256` from the first run, while the host-derived fingerprint fields (Node-binary/boot-id/platform/sandbox-config/launcher-source/policy hashes) stayed identical to the first run as expected on the same unchanged host. `selected-sandbox-launch.test.mjs` re-verified 4/4 after this run; `git status --short` confirmed only the new evidence file was created. | A single run is no longer the case — two independent real opt-in receipts now exist on two different candidates. Neither is yet bound into a frozen Nova A candidate's Verify/Security/Critic gates — that binding, plus final Nova-A candidate-freeze, remain open exactly as for every other row. | Bind the most recent real receipt (`selected-sandbox-launch-record-7936a98.json`, or whichever is current at freeze time) and the unchanged `selected-sandbox-report-57ee7e9.json` into Nova A Verify/Security/Critic once a final candidate is frozen. | Implemented (provisional); open — NVA-A29-6's positive half now real and sealed via two independent real launcher runs (2026-08-11, 2026-08-18); final candidate-freeze binding into Verify/Security/Critic remains open. |
| #38 / Nova A5 invocation reliability | Correlated attempts, pre-launch deterministic rejection, loop prevention, session resolution reuse, privacy-safe telemetry and reproducer-backed repair. | A fresh candidate-bound report is now sealed at `evidence/nova-a/a3/invocation-reliability-report-03c89ae.json`: NVA-A38-1..6 are each mapped to passing focused-suite evidence (`10/10` `invocation-reliability.test.mjs`, `11/11` `invocation-preflight.test.mjs`, re-verified against this exact candidate). NVA-A38-6 is demonstrated only as the code-level four-gate acceptance contract (`A3R09`); no specific prior systemic repair is tracked through it — the fixture uses synthetic placeholder digests, not a real reproducer/fix/fixture/non-recurrence chain for an actual incident. | Final Nova-A binding of the report into Verify/Security/Critic remains open; telemetry still cannot authorize remediation on its own, and no concrete systemic-repair instance backs NVA-A38-6 yet. | Bind the report into Nova A Verify/Security/Critic; if/when a real systemic repair is accepted through this contract, replace the synthetic NVA-A38-6 evidence with its reproducer/fix/fixture/non-recurrence chain. | Implemented (provisional); open. |
| #54 / Nova A6 Critic convergence | Compile/admit reviews, coverage/verdict receipts, delta lineage, four-review/three-correction course gate, closed channels; #75 aggregation and Cyborg isolation remain external. | Critic lineage/economics suites (`12/12`, `103/103`, packet preflight `6/6`) are historical. A real, candidate-bound Critic dispatch is now sealed at `specs/sprint-nova-epic/evidence/nova-a/a5/critic-convergence-report-d5be0e6.json`: `critic-dispatch-preflight.mjs` correctly rejected the dispatch (`CDP-EVIDENCE-REQUIRED`/`CDP-EVIDENCE-BINDING`) until fresh Verify/Security evidence existed for the exact candidate, then admitted it (NVA-A54-1/12 demonstrated); the dispatched review actually reviewed this Nova A evidence-sealing wave (21 enumerated commits, base `6e2c9b2`..candidate `d5be0e69`) and returned PASS with 2 minor findings, each with concrete evidence and a filed disposition (NVA-A54-3/9 demonstrated). **NVA-A54-4/11 now demonstrated too (2026-08-17), by real correction/delta-round cycles that happened for unrelated reasons rather than a manufactured exercise:** `specs/sprint-nova-epic/evidence/nova-a/a5/critic-delta-lineage-report-04a663d9.json` documents two independent real FAIL→fix→re-review cycles this session (ADR-0064/0065 implementation review, 3 findings→0; a GMW guardrail security fix review, 4 findings→0 resolved+1 new). Both round-2 dispatches deliberately bound base/head to the correction commit's own diff rather than the stale round-1 candidate even where unrelated intervening commits existed — a live NVA-A54-4 instance ("exact delta plus impact scope" chosen without needing a typed invalidation to reject a broader range after the fact). The GMW cycle's one new finding (N1) is a textbook NVA-A54-11 instance: a direct consequence of the correction delta itself (the correction's own unfinalized evidence record), clearly distinct from the four historical findings, which stayed dispositions rather than being re-raised. | NVA-A54-5/6/10 remain undemonstrated — confirmed (not assumed) via direct code check: `critic-dispatch-preflight.mjs`, the Claude-native path both new cycles used, does not reference `review-economy.mjs` at all, so the four-round/three-correction cap enforcement (NVA-A54-5), the retry/closed-channel economics reconciliation (NVA-A54-6), and the release-path-specific binding pattern (NVA-A54-10, which needs the actual publication path, not a guardrail-diff review) stay out of reach on this session's own dispatch route — same underlying reason `#98`'s R4 remains open. Full NVA-A54 closure and final Nova-A binding into Verify/Security/Critic remain gated on the eventual Slice A7 candidate freeze, same as every other issue. | Bind into the eventual Slice A7 candidate gate; NVA-A54-5/6/10 need either a Codex-transport session exercising `critic-packet-preflight.mjs`'s `review-economy.mjs`-backed path, or a real publication-path correction cycle for NVA-A54-10 specifically. | Implemented (provisional); open — NVA-A54-4/11 now demonstrated with real evidence (2026-08-17); NVA-A54-5/6/10 remain open, infrastructure-blocked like `#98` R4. |
| #56 / Nova A7 release preflight | Read-only exact-candidate readiness; local vs remote/human gates; endpoint/ref/preimage and sanitized capability cells; no Verify/publication success claim. | `release-preflight-cli.mjs` now exists (2026-08-06, resolving the backlog item that this producer was missing) and was actually exercised — not just unit-tested — against this exact candidate: a fresh candidate-bound report is sealed at `evidence/nova-a/a6/release-preflight-report-57ee7e9.json` (plus its raw builder output `release-preflight-record-57ee7e9.json`). Release-preflight contract suite (`18/18`) and the CLI's own suite (`9/9`) both re-verified. NVA-A56-1..3 and NVA-A56-6 are demonstrated by both focused-suite evidence and the real invocation, which correctly reported `blocked` with `repository-not-clean`/`consent-not-approved` (an honest, non-fabricated verdict — no ready status was manufactured) with version surfaces agreeing (`0.5.2` across all three). NVA-A56-4 and NVA-A56-5 are demonstrated at contract level; a real positive GG-03-binding run could not be produced (no such artifact exists for this candidate) and is reported not-verifiable rather than fabricated. **NVA-A56-7/8 — demonstrated 2026-08-18.** `publication-executor.mjs preflight` (the module that actually implements these cells, per the prior finding) was run for real against candidate `730282fc` (`--remote-name origin --destination-ref refs/heads/main`), independently reproduced twice with byte-identical output, non-mutating (`git status --short` clean before/after both runs). Real result: `status: "ready"`, every cell (`remote`/`credential`/`permissions`/`workflowUpdate`/`policy`/`executor`) resolved `available` or `not-required`, zero `reasons`. Sealed at `evidence/nova-a/a6/publication-capability-preflight-730282f.json`. This demonstrates the remote-capability preflight machinery itself works end-to-end for real — it is NOT a positive GG-03-binding publication run (no push occurred, no PO consent was exercised) and does not by itself change this row's disposition. | Real positive-GG-03-binding run; a genuinely clean+consented real full publication run (this candidate's working tree carries this evidence dispatch's own new files, and no real PO consent record exists yet) is still the remaining gap — PO-only. | Obtain a real approved PO consent artifact and a clean commit point to exercise the ready path for real; then require GG-03 binding before authorization. | Implemented (provisional); open. |
| #98 / Nova A6R + A6S post-v0.4.7 delivery loop and revocation recovery | Rebase/adoption record; productive publication CLI; emergency-override recovery; resumable Full Verify; Critic delta lineage; public release-state consistency; atomic plan-revocation recovery (A6S). | R0 (baseline adoption) and R1 (publication capability preflight) are done, predating the 2026-08-01 snapshot. **A6S's six order steps are functionally complete** (`e09fcee`/`cc25f75`, 2026-08-02): atomic Design postimage, closed legacy plan/apply payload, writer-lock + one-time Human-override-gated apply (`guard-lifecycle-ready.mjs`, the apply command deliberately stays denied outside that one-time capability), receipt-based clearing/zero-write replay, mixed-postimage rejection, and regression coverage — not reflected in this row until now. R5 (public release-state projection) is newly closed 2026-08-07: `docs/release-state.json` regenerated via `createPublicReleaseState()`, `check-release-state-consistency.mjs` clean. | R2's own DoD — "no raw push or improvised library invocation is needed as the normal path" — is directly contradicted by the 2026-08-07 0.5.2 release itself: it shipped via three separate ad-hoc mechanisms (ADR-0056 attested main-push, a GG-03 override, raw `gh release create` for the tag) rather than the one designed `publication-executor.mjs` CLI sequence, which exists (`2fc537d`, pre-snapshot) but was not exercised. **R3 (Verify-resume) is now real and evidenced (2026-08-16):** two real, unmodified `node harness/scripts/verify.mjs` invocations against the same unchanged candidate (`2f34c1cc`), back to back — the first a genuine 269-suite run (198.75s), the second reusing all 269 receipts via `verify-resume.mjs`'s `firstDrift` candidate/implementation/input/log matching (1.84s, ~108x faster; every progress-journal entry self-reports `state:"reused"`). A negative control on the same evidence trail (two DIFFERENT candidates one commit apart) shows zero reuse, confirming the mechanism is neither a no-op nor falsely permissive. Sealed at `specs/sprint-nova-epic/evidence/nova-a/a98/verify-resume-exercise-2f34c1cc.json`. **R4 (Critic delta lineage) claim corrected (2026-08-17): a dedicated module exists and is NOT "still to design/build."** `plugins/pipeline-core/lib/critic-review-lineage.mjs` (created `2fc537d0`, the same pre-snapshot commit already credited for R2's CLI) implements exactly the spec's R4 section (`specs/sprint-nova-epic/design/post-v0.4.7-delivery-loop.md` §R4): lineage-parent binding, delta-scoped correction compilation, the four-round/three-correction course limit, typed invalidation for broad reruns. Its test suite (registered as `nova-critic-lineage-tests` in `verify.mjs`, confirmed passing in every green Verify run this session) covers all five of the R4 negative-matrix rows by name (CRL07 fresh lane/context per correction, CRL08 typed invalidation for broad review, CRL10 hard four-round/three-correction course, CRL12 course-gate parent requirement, CRL13 candidate/impact/finding-scope binding) — 13/13 checks pass. It is wired into `critic-packet-preflight.mjs` (the Codex/packet-transport Critic coordinator), not into the Claude-native `critic-dispatch-preflight.mjs` path this session's own re-reviews used. **What is NOT yet confirmed** (the genuine remaining gap, narrower than the matrix previously stated): a real end-to-end exercise of this lineage enforcement against an actual multi-round release-path Critic correction sequence — analogous to the R3 exercise evidence sealed this same session — has not been produced; only unit-level coverage is confirmed live. **R6 got its first real increment 2026-08-17 (`NVA-A98R6-1`):** `plugins/pipeline-core/scripts/publication-executor-productive-flow.test.mjs` (new, 3/3, independently re-verified, plus the pre-existing `publication-executor.test.mjs` 15/15 confirmed unaffected) chains R1's capability preflight into R2's real v2 operations (`preflight → prepare → authorize-plan → authorize-apply --activate → execute → readback`) as one continuous flow against a disposable local bare-repo remote — extending, not inventing, the fixture pattern the file's own existing tests already use. Side-finding worth carrying forward: reading `publication-executor.mjs`/`publication-authority.mjs` end to end while building this confirmed `authorize-plan`/`authorize-apply` perform NO Ed25519 signature verification today — the real enforced boundary is digest/CAS binding, not a signature — independently corroborating ADR-0064's premise that real PO-signed consent doesn't exist anywhere on this path yet. **Three further increments landed the same day: R3 (`NVA-A98R6-2`, commit `8c2865bf`) backs the fixture's "verify" gate with a real `runVerifyJournal` receipt instead of a hand-asserted pass, with a negative case proving a real failure is actually consulted downstream; R5 (`NVA-A98R6-3`, commit `a4cc2d84`) tags/pushes a real release-state projection to the disposable remote and validates it with the real `checkReleaseStateConsistency` against a fresh independent clone, with a tampered-commit negative case; R0 (`NVA-A98R6-4`, commit `2ef9487d`) performs a genuine `git rebase` inside a fresh disposable repo and validates the resulting receipt by independently re-deriving the base/resultingHead relationship via git plumbing (merge-base + rev-list), not by trusting the receipt's own numbers, with a tampered-OID negative case.** All five non-R4 pieces (R0, R1, R2, R3, R5) are now wired into one continuous fixture (`publication-executor-productive-flow.test.mjs`, 5/5, independently re-verified at each step, sibling suite's 15/15 unaffected throughout). Only R4 remains unwired into this fixture, genuinely blocked — its lineage module requires a Codex-transport "packet" object (real provisioned checkout + cleanup-capability resource) that cannot be safely hand-constructed from a Claude session; confirmed by reading `critic-candidate-packet.schema.json` and `compileCriticReviewLineage`'s exact input contract. | The R2 retroactive exercise was actually attempted 2026-08-07, not just proposed: `publication-executor.mjs preflight` was run for real against the actually-published candidate (`6e2c9b2`, `refs/heads/main`, remote `origin`) and returned genuine `status: "ready"` (sealed at `specs/sprint-nova-epic/evidence/nova-a/a98/publication-capability-preflight-6e2c9b2.json`; `remotePreimage` independently confirms the eventual `execute` push would be a provable no-op). The chain honestly stops at `prepare`, blocked by two real preconditions neither fabricable nor already met: (a) `requireSuccessfulGate`'s critic gate demands a native critic-evidence record with zero findings — the only real candidate-bound Critic review available has 2 (minor, accepted) findings, and using the schema's generic-wrapper escape hatch to force a "passed" critic gate through anyway would be exactly the self-attested fabrication `publication-gate-evidence.mjs`'s own docstring forbids; (b) the real release-preflight evidence (`NOVA-A56-EVIDENCE-1`'s sealed record) genuinely reports `consent-not-approved` for this candidate — no PO consent artifact exists, and none is derivable from anything in the repository. Then design/build R4's delta-lineage module and R6's integrated fixture, and run the complete NVA-A98-1..14 set on a frozen candidate. | Partially implemented; not evidenced/closed — narrower again: A6S functionally done, R0/R1/R3/R5 closed, R2's capability-preflight leg now real and green, R2's prepare/authorize/execute legs genuinely blocked on a zero-findings Critic pass and real PO consent — narrower again as of 2026-08-17: ADR-0064's consent mechanism is no longer just proposed, it is `accepted`, implemented with real production code (`release-preflight-cli.mjs`'s `--proof-request`/`--proof` path plus the Decision-6 hand-supplied-consent waiver tightening), and Critic-passed (round 2, `verdict:yes`) — but no actual PO-signed proof or `waivedKinds` entry has been issued for any real publication candidate, so `release-preflight` consent for a genuine release remains exactly as absent as before, now for lack of an exercised proof rather than for lack of a mechanism. R4's module exists and is unit-tested but not yet end-to-end exercised (narrower gap than previously stated), R6 now chains R0+R1+R2+R3+R5 in one continuous, independently-verified fixture — only R4's own real-data exercise (blocked on Codex-transport infrastructure) and the actual real-PO-consent/zero-findings-Critic preconditions for a genuine publication remain. |
| #60 / Nova B0 runner-native continuation | One runner-neutral generation-bound native goal for Codex/Claude; pause/blocker/control semantics; no authority widening. **NVA-B60-17:** normal bootstrap and Compact re-entry must measure a privacy-safe reproducible payload and stay within a 10–15k-token budget, using modular/lazy loading while retaining lifecycle, authority, calibration, handover, verify and continuation checks; skipped/cached checks do not pass. | Continuation contracts/tests (core `24/24`, Codex host `9/9`, Claude host `4/4`) are local contract evidence only. The payload budget and receipt tests now assert the privacy-safe `utf8-byte-upper-bound` metric, the 15,000-unit hard maximum and normal/Compact receipts; both are registered in Full Verify under the 2026-08-01 TP-3 lift/reinstatement record. | No native goal activation/readback on a frozen Nova candidate, no cross-runner candidate evidence, and no final check-retention manifest/PO activation. | Run NVA-B60-1..18 with both runners on the frozen candidate; capture payload measurements and check-retention manifest, then candidate Verify/Security/Critic and Nova B activation gate. | Implemented (contract-only, including B60-17); open. |
| #21 / Nova B1 local worker pool | Supervised local pool, capacity/pressure/reserved slots, deterministic crash/cancel/orphan handling, real clone/process fixtures, two observed provider-backed workers before advertising. | B1-C/B1-I and process/clone/worktree suites passed in local-readiness audit (`6/6`, `13/13`, `9/9`, `21/21`, `25/25`). | No same-host provider-backed N+1 observation, candidate-bound worker leases or independent final evidence. | Run NVA-B21-1..12 with two certified workers and exact resource envelope; prove supervisor ownership/mode/link checks and then independent Critic/PO gate. | Implemented (local contracts); open. |
| #16 / Nova B2 async execution | Deterministic delayed/duplicate/out-of-order lifecycle; sealed candidate/job broker; provider success remains `succeeded-unverified` until matching metadata. | Async and broker contract fixtures passed (`4/4` plus process-capable CLI fixtures) in local-readiness audit. | No provider job, candidate-bound live readback or final Verify/Critic evidence. | Execute NVA-B16-1..6 with an authorized adapter or explicit unavailable disposition; verify replay/cancel races and sealed predecessor chain. | Implemented (contract-only); open. |
| #18 / Nova B2 credential leases | Brokered short-lived task/repository/operation leases, deny unrelated access, no secrets/private coordinates, CI_JOB_TOKEN-only local contract. | Credential lease contract suite (`4/4`) is local evidence. | No broker activation/canary on the post-rebase candidate; no lease revocation/readback evidence. | Run NVA-B18-1..5 in disposable boundary with secret-scan and denial canaries; bind lease evidence to candidate and Security gate. | Implemented (contract-only); open. |
| #15 / Nova B3-A Antigravity Alpha | Documentation-bound third-runner descriptor (`alpha-documentation-only`), fail-closed unavailable cells; direct AGY execution belongs to #69 (`sprint:NONE`). | The Alpha boundary suite is green (`5/5`) and the candidate-bound descriptor readback is sealed at `evidence/nova-b/b3-alpha-candidate-94880bd.json`. It binds the reviewed B3-R decision, all unavailable privileged cells, and the exact #69 transfer; it records neither AGY execution nor availability. | Sprint-close binding into the eventual Nova B Verify/Security/Critic chain remains open; no AGY execution is a Nova gap. | Retain NVA-B15-1..6 on the frozen final candidate and bind the sealed documentation-only record into the Nova B close evidence. | Candidate-bound documentation-only evidence sealed; final increment evidence open. |
| #51 / Nova B4 forge/transport | Git-only VCS; GitHub/GitLab transport/forge contract, exact target/readback, explicit mutation gates; no silent weaker-control substitution. | Capability/transport/adapter fixtures (`15/15`, `25/25`) and GitLab mutation lifecycle tests are local evidence. | Required live read-only GitLab capability and exact transport readback are absent; no authorized mutation/readback. | Run NVA-B51-1..9 with approved disposable/live read-only target; prove capability cells, ref binding and explicit forge mutation readback. | Implemented (adapter contracts); open. |
| #49 / Nova B5 macOS boundary | Synthetic/non-native macOS contract, bounded keep-awake/resume and nonblocking interaction; native Apple-Silicon transferred to #72 (`sprint:NONE`). | Synthetic macOS boundary (`6/6`) and candidate-freeze regressions exist. Historical V1 remains intact; the post-rebase V2 freeze, exact Verify/Security snapshots, independent Critic evidence and closed seven-source manifest are now versioned under `evidence/nova-b/b5-v2/`, all bound to `7c0b0be…` / `7089d7c…` and released `v0.4.7`. The exact scope-transfer comment is published and read back on #49 as Owner comment `5092523484`; it leaves #49/#72 externally open while assigning native Apple-Silicon closure exclusively to #72. | No Nova B5 gap remains: native Apple-Silicon evidence is deliberately outside Nova, and hosted/Intel evidence may never substitute it. | Retain the read-back transfer reference beside the sealed synthetic-only V2 evidence; #72 owns its later independent candidate and close lifecycle. | Narrowed Nova B5 evidence sealed; Apple-Silicon follow-up is external to Nova. |

## PO-adopted cross-sprint hardening: #61 onboarding

Issue #61 remains its original GitHub/sprint assignment; the PO has explicitly
adopted its runner-neutral deadlock and onboarding hardening into Nova. This
does not change the canonical issue assignment or imply closure by Nova.

| Nova hardening slice | Acceptance boundary | Evidence required before a Nova candidate claim |
| --- | --- | --- |
| NVA-B61-1 canonical generated projection bytes | The shared V3 renderer emits exactly one terminal EOL for `.claude/pipeline.yaml` and `project/pipeline.yaml`; owned sections retain one separator only when followed by another top-level section. No agent manually trims generated output. | Exact LF/CRLF terminal/non-terminal byte tests; a sanctioned projection regeneration changes only owned bytes; direct whitespace drift is typed and the returned lifecycle repair path restores canonical bytes. |
| NVA-B61-2 remote branch adoption before seed | A new Codex folder whose stated intent names an existing remote/branch is classified read-only before portable seed, Git initialization, runtime setup, kickoff, authority migration, or Cleanup binding. The existing branch is adopted into the permitted root and its own authority is then validated. | Fixture for empty host-reserved root plus existing governed remote branch; prove no generic seed/kickoff files, no movement of `.codex`, no invented Cleanup descriptor, preserved branch HEAD, exact target/upstream binding and one bounded confirmation. |
| NVA-B61-3 transaction-owned rollback | A failed or interrupted onboarding transition exposes a typed, bounded rollback/recovery action for exactly the transaction's files. The lifecycle must not make its own rollback impossible merely because it is `partial`, `migration-required`, or a Cleanup observation is unavailable. | Failure injection after each onboarding write; prove no cross-root write, no reserved control-mount move, idempotent rollback/readback, and no external shell repair for a pipeline-created partial state. A migration may not require a private Cleanup authority that did not exist before that migration. |
| NVA-B61-4 new local project before first remote branch | A genuinely new folder whose intended remote branch does not yet exist follows one explicit local-create path: initialize portable authority and Git once, create the first bounded project commit, then present a separate exact remote/branch binding and push plan. It must not require a pre-existing remote branch or loop between seed, kickoff, migration and push guard. | Fixture for empty root plus absent target ref; prove one confirmed initialization transaction, no repeated repair prompt, first commit/readback before push eligibility, typed `remote-branch-create-required` planning, exact destination binding, and a retryable/observable failed remote preflight without local lifecycle deadlock. |
| NVA-B61-5 PO cleanup recovery override | When a Pipeline-created Cleanup state cannot be observed and ordinary `plan-recovery` returns `recovery-unavailable`, the system offers a separate read-only, digest-bound Human recovery plan for the exact repository and cleanup preimage. Its apply requires informed double confirmation, appends a tamper-evident audit record, changes only the bounded Pipeline-owned Cleanup state, and re-inspects lifecycle before permitting ordinary work. | Fixtures for unavailable cleanup observation and absent/invalid plan; prove no manual private-file edit, no generic guard bypass, no publication/push substitution, no `main` destination injection, distinct first/second confirmations, exact audit/readback, and fail-closed refusal when the transaction cannot prove ownership. |
| NVA-B61-6 legacy partial-authority adoption | A legacy repository with `.claude/pipeline.json` but no `pipeline.user.yaml` receives a read-only, preimage-complete planner rather than `partial_authority` with a null action. The PO must explicitly choose the V3 source/profile; apply is digest-bound, rollback-safe and validates V4 after publication. It never invents authority from legacy calibration or overwrites untracked runner projections, skills or handover material on Codex, Claude, Linux, macOS or Windows. | Cross-runner and cross-platform fixtures for legacy calibration plus missing V3 source, untracked partial projections and changed preimages; prove exact plan/choice binding, zero write before confirmation, rollback at each publication stage and typed post-apply V4 result. |
| NVA-B61-7 platform-neutral capability diagnostics and reversible reinstall | A failed session-descriptor probe reports its failed stage (directory hardening/adapter child/publication/load/retire/rollback) through a read-only, redacted plan. The OS adapter records DACL on Windows and the equivalent ownership/mode observation on Linux/macOS without changing the recovery contract. When recovery is undesirable, a separate uninstall-preview distinguishes Pipeline-owned from user-owned files, hashes every target, preserves a restorable quarantine and requires explicit confirmation before any removal. A reinstall starts only after its own readback. | Cross-platform fixtures for inherited/empty descriptor permissions and failing platform adapter child; prove no empty-nextAction loop. Uninstall fixtures prove `.claude` skills, `docs/state.md`, `.agents` and `.codex` user files survive unless each is independently proven Pipeline-owned and explicitly selected. |

The source symptoms are treated as regressions, not user error: repeated
confirmation loops, generic seed/kickoff before known-branch adoption, a busy
Codex control mount, manually corrected generated YAML, a generated
`cleanup_recovery_observation_unavailable` state with no typed repair action,
and a guard that then proposes an unrelated `main` publication route are all
failing acceptance outcomes.

## Recommended implementation order

1. Reconcile #57 first, then certify #7/#29 and #38; these establish the
   canonical record and trustworthy admission/invocation evidence consumed by
   every later gate.
2. Complete #12/#14, #8, #54 and #56, followed by the #98 delivery loop; this
   yields the independently verifiable Nova A increment before any scale or
   provider work.
3. Start Nova B with #60 (including measured NVA-B60-17), then #21, #16 and
   #18; continuation and bounded execution/credentials are dependencies for
   worker and remote lanes.
4. Finish narrowed #15, then #51 and #49; keep #69/#72 and #75 boundaries
   explicit and require fresh Verify/Security/Critic/readback evidence before
   any issue or increment closure.
