# Plan Amendment — Release 0.5.2 Recovery

This repair amendment remains within the approved Sprint Nova lifecycle; it
does not open a separate Design phase.  It is a bounded release repair and
does not include GitHub issue #107.

1. Establish the 0.5.2 candidate baseline and add release-state version-drift
   coverage.
2. Fix issue #100: absent or malformed Push Gate approval becomes a typed,
   fail-closed result.
3. Repair executor external-remote capability preflight and preserve the raw
   push denial boundary.
4. Inventory effectful skill/launcher call sites and add no-start preflight
   assertions where governance, capability or topology evidence is mandatory.
5. Add the native-hook sandbox failure disposition/circuit breaker, then raise
   and align the outer Codex adapter and nested guard budgets with finite
   recovery reserve.
6. Run focused tests, full Verify, Security and a preflighted Critic review;
   make no remote publication in this implementation step.

Completion requires a local, verifiable 0.5.2 candidate.  Publishing to GitHub
is a separately authorized external consequence after candidate acceptance.
