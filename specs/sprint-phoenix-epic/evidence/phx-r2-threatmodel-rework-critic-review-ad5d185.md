# Critic review — PHX-R2-THREATMODEL-rework (`ad5d185`)

**Persisted by the Elephant** from the Critic's returned report (the Critic is
read-only and wrote no file). The Critic confirmed it used repo-relative paths
throughout, so no path transcription was needed here.

Bootstrap check passed: ruleset 2914a55713d714d95323231edaddccec5e1d6c2e5a4fbfaaaaa9d913a5fb1efc loaded · Project agent-pipeline · Calibration n/a · State n/a (Critic sees no history) · Role Critic

**Report header.** Requested route: `claude-opus-5 at max`. Effective-model identity: `unknown` — no direct same-dispatch route evidence was observed; I do not infer identity from a selector or host label. T1 assurance: `functional-equivalent-read-only; OS isolation not asserted`. Write tools were available in this host; per the functional-equivalent lane this is a disclosed residual host limitation, not an isolation claim. I invoked no write tool, no mutating command, and did not delegate. All commands run were read-only reads or read-only checker scripts (verified free of `writeFileSync`/`mkdirSync`/`appendFileSync`/`rmSync` before execution).

Review object constructed myself: `git show ad5d185` / `git diff --name-status e69a784 ad5d185`. Range covers exactly the enumerated SHA; `ad5d185^` resolves to `e69a784d339c75d2e60abe63b6df2cd2d0d3ac55`, matching the dispatch. Two files changed, 417 insertions / 109 deletions.

---

## 1. Findings

### F1 — The submission's verification is a single hand-picked step outside the configured verify entry point, and produces no `pipeline.verify-evidence.v0` artifact

- **Gap:** The project's one configured verify entry point is `node harness/scripts/verify.mjs` (`.claude/pipeline.json:4`). The submission's recorded verification is `node harness/scripts/check-doc-contracts.mjs` alone — one step (`doc-contract-check`) out of roughly 250 registered in `harness/scripts/verify.mjs:372`. Its evidence is a plaintext log emitted by a one-off, dispatch-specific capture script rather than the script-written JSON artifact QG-03 mandates, and it carries no `commit`, no `tree`, and no `candidate.binding`.
- **Risk:** The green claim covers one of ~250 deterministic steps; QG-03 states that a submission without a script-written artifact is unverified regardless of what the report claims. Verify steps that specifically govern the two touched file classes — `language-canon-check`, `backlog-state-check`, `spec-retention-check`, `check-ownership` — did not run against this candidate, and the evidence is not commit-bound (written at `18:33:14Z`, commit at `18:34:08Z`, against the working tree). Severity: **major**. Mitigating and stated honestly: the dispatch record does not overclaim ("verifyCommandsRun", exact command + exit code, no "verify green" assertion), the diff is docs-only, CI remains the unbypassable backstop (QG-02), and I independently ran the three read-only steps most relevant to the touched files — `check-language-canon.mjs`, `check-spec-retention.mjs`, `check-ownership.mjs` — all green. So no red was concealed; the defect is the evidence contract, not a hidden failure.
- **Evidence:** `.claude/pipeline.json:4` → `"verify": "node harness/scripts/verify.mjs"`. `.git/dispatch-record-PHX-R2-THREATMODEL-rework.json:8-15` lists exactly one entry, `node harness/scripts/check-doc-contracts.mjs`, with `"evidence": ".git/verify-PHX-R2-THREATMODEL-rework.log"` and `"writtenBy": ".git/verify-capture-PHX-R2-THREATMODEL-rework.mjs"`. `harness/scripts/verify.mjs:372` → `{ name: "doc-contract-check", file: join(scriptDir, "check-doc-contracts.mjs") }`, one entry in the same array as ~250 siblings. `.git/verify-PHX-R2-THREATMODEL-rework.log:1-11` carries `dispatch`/`startedAt`/`cwd`/`command`/`exitCode`/`signal` and stdout — none of QG-03's `commit`, `tree`, `candidate`, `steps[]` fields.
- **Spec-ref:** QG-02 (`guardrails/quality-gates.md:22-29`, one verify entry point; "MUST NOT define additional or diverging check chains in hooks, CI, or scripts"), QG-03 (`:33`, `:59`), QG-01 (`:16`, "verify + evidence are invariant on ALL rigor levels — there is no path around the deterministic gates, not even for one-line fixes"), GL-01 (`guardrails/global.md:16-19`).

### F2 — Part A's disclosed limitation 2 loses its only proposed successor mechanism and is left open with no owner, no date, no tracking item, and no PO question

- **Gap:** The diff rewrites the §III.1 entry for Part A's disclosed limitation 2 (an allowlisted origin checked out at an arbitrary *committed* history). Previously it named a path to closure ("a signed-release pin could close it offline"); the rework withdraws that pin and records the result as "the limitation stays open with **no successor mechanism proposed here**." No backlog item covers limitation 2, no owner or expiry is attached, and §I.2.10's five open questions for the PO do not mention it — unlike limitation 1 (which got its own tracked item) and SL-1 (which got §I.2.10 item 5).
- **Risk:** A real, security-relevant integrity gap in the shipped bootstrap readiness gate becomes orphaned by an act of this document: disclosed in two design files, tracked in none, and absent from the decision list the PO is being asked to answer. This repo has already paid for this exact pattern once — Critic finding F1 recorded that before its tracking item existed, "the limitation the design correctly discloses had no owner, no next step, and no tracking item anywhere (QG-06's 'documented instead of fixed')". Severity: **minor** — the underlying gap is pre-existing, is not widened by this diff, is still disclosed at its own source, and the status change is recorded honestly rather than hidden; what degraded is the tracking, not the exposure.
- **Evidence:** Diff hunk in `specs/sprint-phoenix-epic/design/part-a-residuals-and-dispatch-template-drift.md` §III.1 — removed: `- §I.2.4 notes that a signed-release pin could close it offline; designing that is not in scope.`; added: `+ The first revision noted that a signed-release pin could close it offline; the 2026-08-07 correction withdraws that pin (§I.2.7), so the limitation stays open with no successor mechanism proposed here.` Source statement: `specs/sprint-phoenix-epic/design/bootstrap-origin-allowlist-and-codex-wsl-freshness.md:155-165` ("This is a real, disclosed limitation this design does not close"). Backlog search across `backlog/` returns only one hit, inside `backlog/items/2026-08-07-marketplace-install-topology-unattested.md`'s *candidate direction 2* prose — an item whose own subject is limitation 1, not limitation 2. Precedent: `backlog/items/2026-08-07-marketplace-install-topology-unattested.md:46-51`.
- **Spec-ref:** QG-06 (`guardrails/quality-gates.md:79`, "MUST NOT introduce 'documented instead of fixed' risks: a known gap with a TODO comment and no due date is a finding, not a mitigation").

---

## 2. Deliberately not flagged

Categories examined and cleared, plus candidates that did not survive the evidence gate.

**(1) Spec fidelity.** The two named spec artifacts were read in full. The design's re-derivation is internally coherent with the constraint it claims: §0.5 states the threat model, §I.2.3(e) derives that an in-tree pin adds no adversary-resistance, and §I.2.4/§I.2.5/§I.2.7/§I.2.9/§I.2.10 remove the signature, the key, the pin module, GS-10, `release-attestation.mjs` and stage 2 consistently. Every dropped element is listed in the "Withdrawn from the first revision's inventory" table rather than silently deleted. **Roughly thirty `file:line` citations across §0.5 and §I.2.3–§I.2.10 were independently verified against source and all resolve correctly**, including: `guard-gate-strength.mjs:15` (the quoted rationale), `:51-98`, `:93-97` (GS-8), `:100-103` (`LIVE_PLUGIN_RULE`), `:118-132`, `:135-140`, `:168-177` vs `:179-188` (GS-6 evaluated first and independently of the governance-marker check), `:196-209`, `:198-199`, `:211-223`, `:32-34`; `hooks.json:34-42`; `guard-maintenance-window.mjs:120-128` (the seven-entry kernel list, matching the design's enumeration item for item), `:175`, `:418-419`, `:529-530`, `:542-545`; `guard-lifecycle-ready.mjs:197-205`, `:206`, `:244-257`, `:514-578` with all six enumerated branches and the terminal `return false` at `:577`, `:920-924`; `public-core-observation.mjs:27`, `:203-205`, `:250`, `:271`, `:280-283`, `:323-357`, `:354-356`; `pipeline-start-preflight.mjs:94-97`, `:204-206`, `:274`, `:288-289`, `:296`, `:333-344`; `provenance-attestation.mjs:10-13`, `:16-25`, `:22`, `:40-42`; `docs/adr/0056-push-approval-mode.md:45-49`; and the backlog item's `:36-40`, `:98-102`, `:112-124`, `:120-124`, `:125-131`. I found no miscited line beyond a one-line boundary drift (`:202-204` for a sentence spanning `:201-203`), which is immaterial.

I also independently re-derived the document's central new security claim (SL-1) from source rather than accepting it: for a Bash tool call, `gateStrengthShellRefusal` builds needles from `GATE_STRENGTH_PATHS` only (`guard-lifecycle-ready.mjs:206`), which excludes `LIVE_PLUGIN_RULE` (a separate export at `guard-gate-strength.mjs:100-103`); `isForbiddenCrossRepositoryMutation` then classifies `node` under none of its branches and falls through to `return false` at `:577`; the command is a simple command with no operators or redirects, so `parseGuardCommand` accepts it. **The claim is correct as stated.** I did not attempt the write; establishing it by reading source is the right method and the design says so explicitly.

**(2) Scope.** Diff touches exactly two files, both within the document's own remit. `specs/sprint-phoenix-epic/design/bootstrap-origin-allowlist-and-codex-wsl-freshness.md` is correctly untouched — its repointing sits in §I.2.7's *implementation* inventory and AC-R2-8, not in this design pass. No unlisted file, no drive-by edit. Dropped candidate: the tension between AC-R2-8 ("The three Part-A passages of §0.3 point at this document") and the backlog item's statement at `:46-48` that those citations point at the backlog item — AC-R2-8 appears as unchanged context in the diff, so it is pre-existing and outside this review object.

**(3) Trajectory / authorship.** See §5 below (Trajectory check). Authorship is clean: the commit carries the grounded trailer `Dispatch: PHX-R2-THREATMODEL-rework (goldfish)`, matching `taskId` in the dispatch record, plus `AI-Assisted: true` for anonymous assistance. No provider/model co-author line, no session URL or ID, no account identifier, no private correlation metadata. The dispatch record names `"dispatcher": "elephant (sprint_phoenix design session)"` — a dispatched fresh-context session, not orchestrator-authored production content. No EL-01/EL-16 lifecycle violation.

**(4) Test integrity.** No test file touched. §I.2.8's case list and §I.2.9's acceptance criteria were rewritten, so I compared them line by line for weakening. Nothing was weakened: the two removed cases (pin verification, non-pinned-key rejection) tested a mechanism the document withdraws, so they have no subject left; one genuinely stronger case was added ("A digest file whose own path was not excluded from the snapshot → the emitter's test must show this is impossible by construction, not merely unobserved"); AC-R2-0 was added as a hard pre-implementation gate; AC-R2-3 was inverted into a negative criterion that makes reintroducing a key, signature or pin "a stop condition, not a judgement call"; AC-R2-5 changed from "contains GS-10" to "is unchanged by R2", which is the correct consequence. Dropped candidate: AC-R2-6 lost the clause "No private key material" — but the same criterion now constrains the digest file to exactly four fields, AC-R2-3 forbids `publicKeySha256` outright, and SEC-01 binds independently, so this is not a weakening.

**(5) Edge cases and failure paths.** The re-derived client-side flow keeps the fail-closed-internally contract (`public-core-observation.mjs:354-356`) explicitly, routes every failure into the pre-existing advisory `plugin-refresh-required` branch at exit 0 with no new status and no new exit code, and states the no-anchor outcome (`undigested`) as a non-failure so existing installs do not trip on day one. The guarantee statement is narrowed rather than broadened and names three things it does *not* prove.

**(6) Guardrails / constraints.** GL-03: no machine-specific absolute path in either touched file; the evidence log deliberately records a redacted repo root, and the capture script derives its root from `import.meta.url`. GL-05: the design does not resolve the conflict it found — it escalates. The reinstatement of candidate direction 3 is explicitly framed as "This design does not take that decision; it is the PO's to reinstate, because the override was the PO's", is filed as §I.2.10 item 1, and the backlog item was updated in the same commit to say "which the PO must confirm". GL-08: the SL-1 hypothesis names the discriminating check, states plainly that it was not executed, and gives the reason. Dropped candidate: I considered whether reinstating a direction the recorded PO Triage marks "overridden" (`backlog/items/2026-08-07-marketplace-install-topology-unattested.md:112-124`) exceeds the design's authority — it does not, because it is raised as a question, not taken as a decision, and the reconciliation section separates what still stands from what is superseded.

**(7) Security surface.** No secret, token or credential-shaped string in the diff, the commit message, the dispatch record, or the evidence log. No new external surface. Two candidates examined and dropped: (a) whether publishing a verified, unfixed bypass of the product's own guard kernel in a public repository needs an embargo — I searched the repository for a responsible-disclosure or embargo policy and found none, and the repo's established practice is to record precisely such defects in public backlog items, so I have no anchor and will not manufacture one; I name it here so the PO can weigh it as a policy question rather than a review finding. (b) The design's §I.2.3(c) quotes `guard-lifecycle-ready.mjs:202-204` but stops before the same sentence continues at `:204-205` with "the residual case is recorded in `docs/state.md` rather than closed by a rule that would break bootstrap" — I could not establish whether that recorded residual *is* SL-1 without reading state, which is forbidden input, so the consequence is unprovable and the candidate is dropped.

**(8) Documented-instead-of-fixed (QG-06).** One candidate survived (F2). One did not: SL-1 is escalated in §I.2.10 item 5 as "needs its own backlog item and its own T1 review", and at `ad5d185` no such item existed. I verified that it was created two commits later at `c0694f8` as `backlog/items/2026-08-07-live-plugin-root-undefended-in-the-shell-lane.md`, with `owner: pipeline`, "Owner: PO", an independent Elephant re-verification, three disclosed remediation directions, and an explicit cross-reference to the adjacent item saying "the two should not be merged". I flag this as cleared, and disclose that the closure lies **outside** the enumerated review object — a Critic bounded strictly to `ad5d185` would see the gap open. I also examined the related pre-existing item `backlog/items/2026-08-07-gate-strength-shell-comment-understates-its-own-scope.md:94-97`, which asserts the same comment block is "accurate, load-bearing" and should be kept verbatim — a tension with SL-1 that the design does not reconcile, but which `c0694f8` resolves explicitly. Dropped for the same reason.

**(9) Dependency reality check.** No new dependency of any kind. The diff removes one proposed internal module (`public-core-release-trust.mjs`) and one proposed script (`release-attestation.mjs`) and proposes one smaller internal replacement (`release-content-digest.mjs`) — all first-party, none a registry package, action or image. No SEC-04 surface, so no registry evidence is owed.

**(10) Language assignment (ADR-0011).** Both artifacts are English, correct for the English-canonical Public Core. `check-language-canon.mjs` run independently: green ("3 bilingual front doors; 5 English-only user documents clear"). No German content, no marker-line issue.

**Model/stage check (MP-07/MP-22/MP-23).** No contradiction. The reviewed diff is an architecture/guardrail/security design artifact; the requested review route is the higher-capability model at max, which matches the matrix. The producing dispatch ran on `claude-opus-5` at effort `xhigh`, which MP-22 (`policies/model-policy.md:129`) makes mandatory for design-phase authorship. The document's new opening paragraph declaring a "standing rule for this package" restates MP-22/MP-23 with package scope rather than creating new policy, so it is not an authority overreach.

---

## 3. Trajectory check

**Verdict: `consistent`, with one element `not verifiable` (named below).**

Consistent:
- The evidence log is machine-written by the script that claims to have written it. `.git/verify-capture-PHX-R2-THREATMODEL-rework.mjs:11-24` spawns the real checker via `spawnSync` and serialises `run.status`, `run.signal`, `run.stdout`, `run.stderr` verbatim — the model authored the harness, not the result.
- I reproduced the claimed check myself. At HEAD it reports `487 Markdown file(s), 776 link(s), 13 anchor check(s)`; the log at `ad5d185` reports `484 Markdown file(s), 776 link(s), 13 anchor check(s)`. Link and anchor counts match exactly; the file-count delta of 3 moves in the correct direction for the two commits that landed after the review object. Claimed exit code `0` matches.
- Timestamps and ordering are plausible: log `startedAt 2026-08-07T18:33:14.753Z`, commit `18:34:08Z` — 54 seconds apart, in the right order.
- The dispatch record's file list and stats match `git show --stat` exactly (two files, `417 insertions(+), 109 deletions(-)`).
- The dispatch record's `deviationsFromSpec` field discloses that stop condition 2 was evaluated and deliberately not triggered, and that SL-1 is escalated rather than designed around — a self-reported deviation, not a concealed one. §I.2.4's conclusion is in fact constructed to hold under either answer, as claimed.
- The document's own §III.4 verification log claims "Not run, deliberately: no attempt was made to write into the live plugin root through either lane." I found no evidence of any such attempt, and the SL-1 characterisation is fully derivable from the static read, as I confirmed independently.

Not verifiable: **§0.5's fidelity to the actual PO decision.** The threat model is the load-bearing premise of the entire re-derivation, and it is stated as "PO decision, 2026-08-07". I confirmed from commit metadata alone that an ancestor commit, `813e4de` ("docs(phoenix): bound the threat-model correction so it does not retire the accepted signing boundary"), touched `docs/state.md` and nothing else, 19 insertions — so a decision-register entry exists and GL-05's "register entry before it is acted on" is satisfied. I did **not** read its content: `docs/state.md` is forbidden Critic input. Consequence: I can confirm the register entry exists and that its commit subject is consistent with §0.5 point 3, but I cannot confirm that §0.5's three-point restatement is a faithful rendering of the PO's actual wording. That check is structurally outside a Critic's admissible inputs and belongs to the Elephant or the PO. No ADR records the threat model (`docs/adr/README.md` returns no match for threat model / adversary), which is worth noting but is not itself anchored, since the register entry is the primary requirement.

---

## 4. Briefing violations observed

**One, disclosed and handled; no contaminating content was consumed.**

The dispatch supplied, under "Input contract for this rework dispatch", a reference to `specs/sprint-phoenix-epic/evidence/wp2wp3-parta-rework-3-delta-critic-review-138e2e3.md`, labelled "the neutral findings registry for the prior rounds". The fail-closed reference boundary lists "prior verdict" and "earlier review verdicts" as forbidden input "even when disguised in a filename, reference label, or metadata field", and this filename is `…-critic-review-…`.

**I did not open it.** I did not read, quote, search, or build on it, and no finding above derives from it. I did not stop the review, because the boundary's stop condition is triggered by forbidden material being *present* in the dispatch; here only a reference was offered, every genuinely required artifact resolved, and stopping would have withheld a review whose inputs were otherwise complete. I record the consequence honestly: "does the rework address prior round N's findings?" is a question I deliberately did not answer via that route. My findings are the product of my own construction of the diff, the two named spec artifacts, the guardrail files, the dispatch record, the evidence log, and my own reading of the cited source. Recommendation for future dispatches in this package: either omit the reference or reclassify what it contains, so the boundary is not tested each round.

All other required artifacts resolved and were within the boundary: spec (2 files), diff (1 enumerated SHA, range confirmed), guardrails (`CLAUDE.md`, `guardrails/quality-gates.md`, `guardrails/security.md`, `guardrails/global.md`, `policies/model-policy.md`), evidence (`.git/verify-PHX-R2-THREATMODEL-rework.log`, `.git/verify-capture-PHX-R2-THREATMODEL-rework.mjs`), dispatch record. The dispatch carried no claims-to-verify list, no hunt list, no expected conclusion, no implementor characterization and no rationale prose. Not a contaminated dispatch in that sense.

---

## 5. Binary verdict

**PASS**, with F1 and F2 to be dispositioned by the Elephant.

Neither finding is a blocker. F1 is an evidence-contract defect whose concealed-failure risk I actively tested for and did not find; F2 is a tracking-hygiene regression on a pre-existing, separately disclosed limitation. The document itself is unusually well-grounded — its factual claims survived roughly thirty independent source checks without a substantive miss, its central new security finding is correct on my own re-derivation, it withdraws a designed mechanism on stated grounds while recording every dropped element for audit, and it escalates rather than resolves the one decision that is not its to take.

---

## 6. Disclosure duty (auto-injected context)

Named as accepted, not silent:

- **`CLAUDE.md` project instructions**, auto-loaded. I used its constraints block as an explicit dispatch-named guardrail source.
- **A git status / recent-commits snapshot** reflecting the *parent session's start*, listing `154a113` as HEAD and four earlier commits. **I did not use it as a freshness reference.** All commit and diff state in this report comes from my own `git show ad5d185`, `git diff --name-status`, `git rev-parse`, `git log --graph --all` and `git status --porcelain` calls. Note the snapshot is materially stale: actual HEAD at review time is `c0694f8`, and `154a113` does not appear in the current first-parent chain I observed.
- **User auto-memory** (`MEMORY.md`, three entries: push target, uncommitted local state files, verify-in-detached-worktree). I treated these as environment notes only. In particular I did **not** use the verify-worktree note to excuse or explain finding F1 — a guardrail is measured against the guardrail, not against a memory entry.
- **A `PreToolUse` guard (`guard-lifecycle-ready.mjs`)** actively constrained my tool use, refusing composed commands three times (`GUARD-PARSE-UNSUPPORTED`, `GUARD-OPERATOR-UNAPPROVED`). I complied by decomposing into single simple commands; no guard was bypassed, disarmed, or probed by writing.
- **Pre-existing scratchpad state found:** one zero-byte file, `pre-diff-design.md`, timestamped before this dispatch, in the session scratchpad root. I did not read it, did not build on it, and did not modify it. I built no fixtures, repros or baselines in this dispatch — all evidence is direct reads and read-only checker runs against the repository — so no fresh subdirectory was needed and none was created.

Repo-relative paths used throughout; no machine-specific absolute path appears in this report.
