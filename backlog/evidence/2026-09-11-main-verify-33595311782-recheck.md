# Main Verify run 33595311782 recheck

- GitHub Actions run: `33595311782`
- Job: `100137458431`
- Main commit: `6262d408aa616651232b46ab8ecbfd88ce4055b0`
- Historical result: 503/506 Core Verify suites

The three historical failures and their current ancestor fixes are:

1. `guard-lifecycle-ready-tests`, rebase requirement 5-2: the synthetic CI
   PATH could not resolve `GIT_EDITOR=true`. Commit `216ff054` provides the
   test-owned executable shim.
2. `codex-onboarding-capabilities-tests`: a directory snapshot raced Git's
   disappearing `.git/objects/maintenance.lock`. Commit `ae8da7b8` handles the
   narrow transient Git-lock case.
3. `local-worker-supervisor-cli-tests` LWSC04: heartbeat age changed the record
   digest before cancellation. Commits `91f9bc45` and `fc04dbc8` correct the
   product binding and polling fixture.

A read-only diagnostic rebuilt the workflow's five-tool PATH containing only
Node, Git, Bash, sh and OpenSSL, then reran the three targets. Results were 1/1,
30/30 and 10/10 green. The current branch contains every named fix.

This evidence is deliberately narrow. It establishes that the old run's three
failures have direct fixes and pass under its relevant local environment. It
does not replace a fresh push-bound Verify for the current candidate, which is
hundreds of commits ahead. No push or workflow mutation was performed.
