# ADR Contracts and Critic vendor integration

The sanctioned generator's non-writing manifest found exactly these two stale
consumer destinations, and no others:

- `plugins/pipeline-core/docs/adr/0013-git-guard-union.md`
- `plugins/pipeline-core/docs/adr/0014-critic-contract.md`

Both are generated byte-identical copies of their accepted root-canon ADRs.
The final generator check is captured at
`scratch/NVA-B-ADR-GOVERNS-VENDOR-8/generator-check.json`.

The pre-allowance consumer check found one real inherited source-only header:
ADR-0014's complete `Governs:` line includes `harness/` paths. ADR-0013's
header has no configured source-only prefix, so it received no consumer-checker
entry. The checker retains its existing exact file-plus-substring matcher; this
classification adds only ADR-0014's complete inherited header as a Class B
vendored-canon entry. It adds no whole-file exemption, regex waiver, or matcher
change.

`scratch/NVA-B-ADR-GOVERNS-VENDOR-8/consumer-pre-allowance.json` preserves the
actual red finding. `scratch/NVA-B-ADR-GOVERNS-VENDOR-8/check-final.json`
reconstructs that rejection with the new entry removed and proves that a
separately appended consumer instruction remains rejected in ADR-0014. The
final consumer checker, its direct nine-test suite, and document-contract check
passed in their respective machine captures.

The terminal roots were CONTRACTS-8
`1d191a56941cfe2c767edab867fe60a37c0dd05f` and PRACTICE-8
`9742b1aaa8b2f2502bd9d4a42285ede411034967`. The proof records both current
source hashes and their differing historical rows. No source ADR, runtime,
guard, authority, pin, schema, or test changed. Candidate-wide verification
and independent review remain with the parent; no T0 Critic skip is asserted.
