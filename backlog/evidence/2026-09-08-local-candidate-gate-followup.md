# Local candidate gate follow-up — 2026-09-08

The exact full Verify on `94bbc6a6ac0e17c91af08e1370810d5b06e55af3`
finished with exit 1: 505 of 517 suites passed. Its preserved machine record
is `evidence/verify-1788823644708-c3279dedde58cddd.json`. The suite count is
not a count of independent defects. No later complete gate or selected real
Critic PASS is claimed.

Subsequent local repairs include:

- `d1617a3d17305ed0a739bf4dac4a23ac79fb214d`: HGO consumer fixtures retain
  signing and handoff coverage using an eligible supplied denial, alongside
  nonliftable lifecycle assertions.
- `f0bb049bc54fcaf088b8a595c80c28f48f32802b`: existing neutral mirrors
  participate in the authenticated V3 migration, with preimages and recovery.
- `505a2e0fd5f470c7089dc217e64198b6349bacc5`: routing projection expectations
  derive from governing sources; historical alias and host-evidence tests remain.
- `55e29d097a79af9e28fdb0d99d729eaebfbc46a6`: historical V2 usage receipts
  retain exact binding; native Antigravity cache fields preserve provenance,
  zero and omission. Current V3 project-selected metering remains separate.

The focused terminal captures referenced in the individual evidence files
prove those repairs. The actual local migration updated only the existing
project YAML mirror. Routing, language and authority-tier checks now pass
against that working tree. The mirror commit was refused by GS-3; therefore
those working-tree results do not prove a clean final candidate.

## Collected PO boundaries

1. QG-16 requires explicit action approval, separate from review-launch consent,
   for four Critic integrity pins. Independent provenance inspection and exact
   proposed hashes exist; the real pins remain unchanged.
2. TP-10 refused the protected notebook-coverage test correction. NB02 treats
   TodoWrite as Write by substring. A corrected exact-token patch is prepared
   for external Author-Repair signing. Earlier request/plan hashes are historical
   and cannot authorize changed patch bytes.
3. GS-3 refused committing the generated project manifest. Its actual diff
   changes only the already approved model projection, but file-wide protection
   requires the human operator. No agent hook bypass was attempted.

Concrete operator material and fresh-binding prerequisites are collected in
`scratch/po-candidate-queue-20260908.md`. No signature request has been prepared
for consumption while unrelated work continues. No release, push, installation,
GitLab write, live supervisor run or alternative Critic approval is claimed.

Independent approved Nova-B coverage work continues. D.2–D.6 remain scheduled
after the test candidate; incomplete work and the historical Greenfield
intermittence must remain visible in the final operator handover.
