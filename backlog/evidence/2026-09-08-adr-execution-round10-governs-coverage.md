# ADR execution Governs coverage — round 10

This record documents four source ADR headers. Each header names current, tracked implementation and verification paths; the headers do not alter the accepted decisions.

| ADR | Rationale | Material omission or boundary |
| --- | --- | --- |
| ADR-0044 | The frozen DTO is implemented by the exchange and continuity modules, its schema, namespace registry and fixture corpus. The lifecycle mapper and `pipeline-state` are the current direct event-projection/caller path, with their direct tests. | It has no vendored target: the canon manifest's universal ADR list omits ADR-0044. Provider execution and a new authority store remain outside the decision. |
| ADR-0048 | The header follows the decision's immutable B1-I path manifest: supervisor contracts, schema, CLI, deterministic worker fixture, threat model, capability/Verify registration and named Nova artifacts. | The historical `.claude/pipeline-state.json` manifest entry is not tracked in the current repository. No substitute is named; later changes require a separate decision or manifest amendment. |
| ADR-0049 | The closed broker state machine, CLI, schemas, deterministic fixture, threat model, Verify registration and B2-I planning/acceptance artifacts implement the constrained GitLab pilot. | Imported GitLab read observations are deliberately excluded: they do not prove CI/worker execution. Generic forge helpers are separately owned and are not claimed as GitLab-pilot-specific paths. |
| ADR-0062 | The real execution contract and launcher, selected-sandbox driver, their direct tests, schema, threat model, Verify registration and acceptance matrix are the actual extension inside ADR-0044's frozen boundary. | The existing selected-sandbox reducer is intentionally excluded: ADR-0062 authorizes the missing driver and explicitly preserves that reducer contract. Remote/provider execution remains unapproved. |

The raw search and manifest captures are retained separately at `scratch/NVA-B-ADR-GOVERNS-EXECUTION-10/raw-source-capture.md`. The original scoped check captures the declarations, shared status classification and local 4/4 increment. Its own glob implementation does not establish that every declaration matches a tracked target; its historical body comparison does not independently establish the stated fixed commit baseline.

The parent closed both verification gaps before integration with `scratch/check-round10-source-parent-20260908.mjs` (exit 0). This uses the actual reconciliation module's `parseGovernsGlobs` and `globMatches`, requires at least one tracked nonempty file for every declaration, and compares each header-stripped document directly against `git show cfd8804c417da105ab47e1b484664d6d4ec94827:<path>`. All twelve round-10 documents passed; exact current file hashes and per-glob matches are retained in `scratch/round10-parent-source-verification-20260908.json`. This proves the declared source scope and preserved bodies, not candidate-wide conformance or reconciliation.
