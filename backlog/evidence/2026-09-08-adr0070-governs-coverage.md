# ADR-0070 Governs coverage

ADR-0070 now declares one exact parser-recognized `Governs` line containing
the eleven non-self paths from its accepted twelve-path implementation
manifest. The existing accepted status, decision, obligations, rollback, and
runtime text are unchanged.

Evidence: `scratch/NVA-B-ADR0070-GOVERNS-1/check-capture.json`, generated by
`node scratch/NVA-B-ADR0070-GOVERNS-1/check.mjs` through
`capture-evidence.mjs`, exit code 0. The parser returned exactly eleven globs;
all eleven matched tracked existing paths. This is one previously uncovered
accepted ADR improved; the remaining coverage queue and final reconciliation
record are parent-owned.
