#!/usr/bin/env node
// SPDX-License-Identifier: SUL-1.0

import assert from "node:assert/strict";
import { createHash } from "node:crypto";
import { afterEach, test } from "node:test";
import { mkdirSync, mkdtempSync, readFileSync, rmSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { run } from "./pipeline-state.mjs";

const roots = []; const NOW = "2026-07-31T20:00:00.000Z";
const sha = (x) => createHash("sha256").update(x).digest("hex");
const h = (x) => x.repeat(64);
const canonical = (v) => v === null || typeof v !== "object" ? JSON.stringify(v) : Array.isArray(v) ? `[${v.map(canonical).join(",")}]` : `{${Object.keys(v).sort().map((k) => `${JSON.stringify(k)}:${canonical(v[k])}`).join(",")}}`;
afterEach(() => { while (roots.length) rmSync(roots.pop(), { recursive: true, force: true }); });
function invoke(root, argv, deps = {}) { const out = []; const err = []; const log = console.log; const error = console.error; console.log = (...x) => out.push(x.join(" ")); console.error = (...x) => err.push(x.join(" ")); try { return { status: run(argv, { dir: root, now: () => NOW, gitCommonDir: () => ({ ok: true, path: join(root, ".git-common") }), ...deps }), out: out.join("\n"), err: err.join("\n") }; } finally { console.log = log; console.error = error; } }
function fixture(name) {
  const root = mkdtempSync(join(tmpdir(), `pipeline-case-migration-${name}-`)); roots.push(root); const dir = join(root, "specs", "feature"); mkdirSync(join(root, ".claude")); mkdirSync(join(root, ".git-common")); mkdirSync(dir, { recursive: true });
  const spec = Buffer.from("# Spec\n"); const specSha256 = sha(spec); const prd = Buffer.from(`<!-- technical-spec-sha256: ${specSha256} -->\n# PRD\n`); const prdSha256 = sha(prd); const lower = Buffer.from("# historical lower\n"); const upper = Buffer.from("# canonical upper\n");
  writeFileSync(join(dir, "spec.md"), spec); writeFileSync(join(dir, "prd_feature.md"), prd); writeFileSync(join(dir, "result.md"), lower); writeFileSync(join(dir, "Result.md"), upper);
  const authority = { schema: "pipeline.po-gate-authority.v2", humanFacing: "en", sourceSha256: h("1"), runtimeSha256: h("2"), receiptSha256: h("3"), repositoryFingerprint: h("4"), planPath: "specs/feature/prd_feature.md", planSha256: prdSha256, specPath: "specs/feature/spec.md", specSha256 };
  const submission = { schema: "pipeline.plan-submission.v1", featureId: "feature", planPath: authority.planPath, planSha256: prdSha256, specPath: authority.specPath, specSha256, profile: "feature", profileSha256: h("5"), submittedBy: "Coordinator", submittedAt: "2026-07-31T19:00:00.000Z" };
  const approval = { schema: "pipeline.plan-approval.v4", approvedBy: "PO", approvedAt: "2026-07-31T19:05:00.000Z", submissionSha256: sha(canonical(submission)), profileSha256: submission.profileSha256, poGateAuthority: authority, priorInvalidationSha256: null };
  const state = { schema: "pipeline.state.v0", activeFeature: { id: "feature", planPath: authority.planPath, phase: "implementation" }, planApproved: true, planSubmission: submission, planApproval: approval, continuity: { schema: "pipeline.continuity.v0", featureId: "feature", revision: 7, runtime: { humanFacingLanguage: "en", activeDuty: "Coordinator", sessionCleanup: null }, authority: { prd: { path: authority.planPath, sha256: prdSha256 }, spec: { path: authority.specPath, sha256: specSha256 }, result: { path: "specs/feature/Result.md", sha256: sha(upper) } }, queueHead: { packageId: "pkg", actionId: "review", nextAction: "review", productRetryCount: 0, environmentRerouteCount: 0, dispatch: null }, blocker: null, acknowledgedFinal: null, resume: { mode: "immediate", sourceRevision: 7, reasonCode: "active-turn" }, recovery: null, decisionTxn: null, closeTransition: null, capacity: { concurrencyLimit: 4, reservedCriticSlots: 1, reservedRecoverySlots: 1, fallbackPolicy: "defer" } }, updatedAt: "2026-07-31T19:10:00.000Z" };
  const lifecycle = { schema: "pipeline.feature-package.v1", feature: { id: "feature", rigor: 1 }, state: "implementing", artifacts: [{ class: "prd", path: authority.planPath, sha256: prdSha256, authority: true, mutability: "immutable", retention: "active" }, { class: "spec", path: authority.specPath, sha256: specSha256, authority: true, mutability: "immutable", retention: "active" }, { class: "result", path: "specs/feature/result.md", sha256: sha(lower), authority: true, mutability: "append-only", retention: "active" }], candidate: null, supersedes: null };
  const statePath = join(root, ".claude", "pipeline-state.json"); const lifecyclePath = join(dir, "lifecycle.json"); writeFileSync(statePath, JSON.stringify(state, null, 2) + "\n"); writeFileSync(lifecyclePath, JSON.stringify(lifecycle, null, 2) + "\n");
  return { root, dir, state, statePath, lifecyclePath, lower, upper, lowerPath: join(dir, "result.md"), upperPath: join(dir, "Result.md"), archivePath: join(dir, "archive", "result.md") };
}
function plan(f) { const state = readFileSync(f.statePath); const lifecycle = readFileSync(f.lifecyclePath); const r = invoke(f.root, ["continuity-result-case-migration-plan", "--result-path", "specs/feature/Result.md", "--archive-path", "specs/feature/archive/result.md"]); assert.equal(r.status, 0, r.err); assert.deepEqual(readFileSync(f.statePath), state); assert.deepEqual(readFileSync(f.lifecyclePath), lifecycle); return JSON.parse(r.out); }

test("Phoenix migration rejects candidate, lifecycle, State and archive drift and recovers a source-moved journal", () => {
  for (const mode of ["state", "lifecycle", "candidate", "archive"]) { const f = fixture(mode); const p = plan(f); if (mode === "state") writeFileSync(f.statePath, Buffer.from("{}\n")); if (mode === "lifecycle") writeFileSync(f.lifecyclePath, Buffer.from("{}\n")); if (mode === "candidate") writeFileSync(f.upperPath, Buffer.from("changed\n")); if (mode === "archive") { mkdirSync(join(f.dir, "archive")); writeFileSync(f.archivePath, Buffer.from("occupied\n")); } const r = invoke(f.root, p.applyAction.argv.slice(1)); assert.equal(r.status, 2, mode); assert.doesNotMatch(r.out, /"status":"(?:applied|replayed)"/); }
  const f = fixture("recovery"); const p = plan(f); const first = invoke(f.root, p.applyAction.argv.slice(1), { afterCaseMigrationSource: () => false }); assert.equal(first.status, 2); assert.deepEqual(readFileSync(f.archivePath), f.lower); const second = invoke(f.root, p.applyAction.argv.slice(1)); assert.equal(second.status, 0, second.err); assert.equal(JSON.parse(second.out).status, "replayed"); assert.deepEqual(readFileSync(f.upperPath), f.upper); assert.throws(() => readFileSync(f.lowerPath)); const state = JSON.parse(readFileSync(f.statePath)); assert.equal(state.continuity.authority.result.path, "specs/feature/Result.md"); assert.equal(state.continuity.revision, 8); const lifecycle = JSON.parse(readFileSync(f.lifecyclePath)); assert.deepEqual(lifecycle.artifacts.filter((x) => x.class === "result").map((x) => [x.path, x.authority, x.retention]), [["specs/feature/archive/result.md", false, "archive"], ["specs/feature/Result.md", true, "active"]]); const replay = invoke(f.root, p.applyAction.argv.slice(1)); assert.equal(replay.status, 0, replay.err); assert.equal(JSON.parse(replay.out).status, "replayed");
});
