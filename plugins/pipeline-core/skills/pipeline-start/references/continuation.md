# Continuation reference (lazy)

Load only for `PCR-BLOCKED` or `PCR-DECISION-PENDING`, or when a named PO gate,
typed blocker, pause, cancel, replace, redirect, resume, or Compact control is
being handled. Preserve the exact feature, phase, plan/Spec, action, revision,
goal generation and evidence binding. A blocker is not a completion claim and
ordinary Compact re-entry must not create a successor generation.

