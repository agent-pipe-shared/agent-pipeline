// SPDX-License-Identifier: SUL-1.0
/** Offline static HTML renderer for the non-authoritative PHX-5 view model. */
function esc(value){return String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#39;");}
export function renderEvidenceView(model){if(model?.schema!=="pipeline.evidence-view-model.v1"||model.authority!=="non-authoritative"||!Array.isArray(model.artifacts))throw new TypeError("EVR-MODEL");const rows=model.artifacts.map(a=>`<tr><td>${esc(a.path)}</td><td>${esc(a.state)}</td><td><code>${esc(a.sha256)}</code></td></tr>`).join("");return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'"><title>Pipeline Evidence</title><style>body{font:16px system-ui;margin:2rem;max-width:72rem}table{border-collapse:collapse;width:100%}td,th{border:1px solid #777;padding:.5rem;text-align:left}code{overflow-wrap:anywhere}</style></head><body><main><h1>Evidence view</h1><p>Authority: non-authoritative projection</p><p>Status: <strong>${esc(model.status)}</strong></p><p>Candidate: <code>${esc(model.candidate.commit)}</code> / <code>${esc(model.candidate.tree)}</code></p><table><caption>Validated artifact states</caption><thead><tr><th>Artifact</th><th>State</th><th>Digest</th></tr></thead><tbody>${rows}</tbody></table></main></body></html>`;}
