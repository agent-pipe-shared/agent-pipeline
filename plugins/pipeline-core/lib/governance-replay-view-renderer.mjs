// SPDX-License-Identifier: SUL-1.0
/** Static, offline renderer for the non-authoritative governance replay view. */
import { readFileSync } from "node:fs";
import { fileURLToPath } from "node:url";
import { join } from "node:path";

const CSS = readFileSync(join(fileURLToPath(new URL(".", import.meta.url)), "..", "assets", "evidence-viewer.css"), "utf8");
function esc(value) { return String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;"); }
function tag(value, kind = "fact") { return `<span class="value value-${esc(kind)}" data-value-class="${esc(kind)}">${esc(value)}</span>`; }
function eventRows(timeline) { return timeline.events.map((event) => `<tr><td><code>${event.sequence}</code></td><td>${tag(event.kind)}</td><td>${tag(event.status, event.status)}</td><td><code>${esc(event.reasonCode)}</code></td><td><code>${esc(event.correlation.packageId)}</code> / <code>${esc(event.correlation.workerId)}</code> / <code>${esc(event.correlation.attemptId)}</code></td><td><code>${esc(event.eventId)}</code>${event.invalidatesEventId === null ? "" : ` → <code>${esc(event.invalidatesEventId)}</code>`}${event.supersedesEventId === null ? "" : ` ↝ <code>${esc(event.supersedesEventId)}</code>`}</td></tr>`).join(""); }
function timelineBlocks(model) { return model.timelines.map((timeline) => `<article id="dispatch-${esc(timeline.dispatchId)}"><h3><code>${esc(timeline.dispatchId)}</code> ${tag(timeline.status, timeline.status)}</h3><table><caption>Verified lifecycle events in sequence order</caption><thead><tr><th scope="col">Sequence</th><th scope="col">Kind</th><th scope="col">State</th><th scope="col">Reason</th><th scope="col">Topology</th><th scope="col">Link</th></tr></thead><tbody>${eventRows(timeline)}</tbody></table></article>`).join(""); }
function topologyRows(model) { return model.topology.map((node) => `<tr><th scope="row"><code>${esc(node.packageId)}</code></th><td><code>${esc(node.workerId)}</code></td><td><code>${esc(node.attemptId)}</code></td><td>${node.dispatchIds.map((id) => `<a href="#dispatch-${esc(id)}"><code>${esc(id)}</code></a>`).join(", ")}</td><td>${node.eventCount}</td></tr>`).join(""); }

export function renderGovernanceReplayView(model) {
  if (!model || model.schema !== "pipeline.governance-replay-view.v1" || model.authority !== "non-authoritative" || !["observed", "unavailable"].includes(model.state) || !Array.isArray(model.timelines) || !Array.isArray(model.topology)) throw new TypeError("GRVR-MODEL");
  const overview = model.state === "unavailable" ? `<p>${tag("unavailable", "unavailable")}: ${esc(model.reason)}. No partial timeline is rendered.</p>` : `<dl><dt>Dispatch timelines</dt><dd>${model.timelines.length}</dd><dt>Topology nodes</dt><dd>${model.topology.length}</dd><dt>Human authority</dt><dd>${tag("unavailable", "unavailable")} — Cyborg proof is not inferred from replay.</dd></dl>`;
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'"><title>Governance Replay</title><style>${CSS}</style></head><body><a class="skip-link" href="#content">Skip to replay</a><header><h1>Governance Replay</h1><p>${tag("non-authoritative local projection")}</p><nav aria-label="Replay sections"><a href="#overview">Overview</a><a href="#timelines">Timelines</a><a href="#topology">Topology</a><a href="#limits">Limits</a></nav></header><main id="content" tabindex="-1"><section id="overview" aria-labelledby="overview-title"><h2 id="overview-title">Replay state</h2>${overview}</section><section id="timelines" aria-labelledby="timelines-title"><h2 id="timelines-title">Dispatch timelines</h2>${model.state === "observed" ? timelineBlocks(model) : ""}</section><section id="topology" aria-labelledby="topology-title"><h2 id="topology-title">Package, worker and attempt topology</h2>${model.state === "observed" ? `<table><caption>Observed correlation nodes; this is not a worker-identity or authority assertion.</caption><thead><tr><th scope="col">Package</th><th scope="col">Worker</th><th scope="col">Attempt</th><th scope="col">Dispatches</th><th scope="col">Events</th></tr></thead><tbody>${topologyRows(model)}</tbody></table>` : ""}</section><section id="limits" aria-labelledby="limits-title"><h2 id="limits-title">Limits</h2><p>Replay only displays the verified lifecycle allowlist. It cannot approve work, establish a human decision, repair canonical data, or turn an unavailable stream into a partial history.</p></section></main></body></html>`;
}
