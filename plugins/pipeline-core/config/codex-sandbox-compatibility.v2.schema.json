{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pipeline.codex-sandbox-compatibility-policy.v2",
  "title": "Closed semantic Codex sandbox compatibility policy",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "maxEvidenceAgeMs", "fallback", "entries"],
  "properties": {
    "schema": { "type": "string", "const": "pipeline.codex-sandbox-compatibility-policy.v2" },
    "maxEvidenceAgeMs": { "type": "integer" },
    "fallback": {
      "type": "object",
      "additionalProperties": false,
      "required": ["laneId", "runnerId", "assuranceLiteral", "allowedFailureCodes"],
      "properties": {
        "laneId": { "type": "string" },
        "runnerId": { "type": "string" },
        "assuranceLiteral": { "type": "string", "const": "functional-equivalent-read-only; OS isolation not asserted" },
        "allowedFailureCodes": { "type": "array", "items": { "type": "string", "enum": ["binary-missing", "unsupported-profile", "sandbox-setup-error", "permission-denial", "child-stdio-error"] } }
      }
    },
    "entries": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "runnerId", "primaryLaneId", "fallbackLaneId", "compatibilityClass", "kernelClass", "filesystemClass", "permissionProfileId", "permissionProfileSha256", "preflightSchemaSha256", "networkEnabled", "initialState"],
        "properties": {
          "id": { "type": "string" },
          "runnerId": { "type": "string" },
          "primaryLaneId": { "type": "string" },
          "fallbackLaneId": { "type": "string" },
          "compatibilityClass": { "type": "string", "const": "codex-sandbox-state-v1" },
          "kernelClass": { "type": "string" },
          "filesystemClass": { "type": "string", "enum": ["native-linux", "wsl-native", "drvfs"] },
          "permissionProfileId": { "type": "string" },
          "permissionProfileSha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "preflightSchemaSha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "networkEnabled": { "type": "boolean" },
          "initialState": { "type": "string", "enum": ["diagnostic-only", "intermediate-preflight-candidate"] }
        }
      }
    }
  }
}
