<!-- po-language: de -->

# Sentinel Platform-Support-Contract

Archivierte Fassung der im PRD gebundenen Plattformautorität vom 2026-07-22.
Sie führt Windows, Linux, WSL und macOS als getrennte native Evidenzklassen;
im Windows-Track ist `#33` geschlossen, `#34`–`#37` bleiben offen. Docker
bleibt out of scope und ist kein Ersatz für native same-surface evidence.
Siehe die aktive, SHA-256-gebundene Autorität unter
`specs/2026-07-19-sprint-sentinel-epic/platform-support-contract.md`.
